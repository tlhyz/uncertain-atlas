# 反模式：最大消息尺寸被写成接收分配上限

> 真值：[CVE-2015-3641](../../tracks/failure-museum/cve-2015-3641.md)、[不变式 48](../invariants/README.md#48-接收缓冲必须在读完载荷前有界)。

## 一句话

规范写了对象最大多少字节，文案就写成「每连接接收 RAM 已经有上限」。

## 正确写法

「最大序列化尺寸 ≠ 读完载荷前肯分配的字节。未完成的大消息可以按每连接钉 RAM。」
