# 反模式：提前失败被写成后台作业已经汇合

> 真值：[CVE-2024-52911](../../tracks/failure-museum/cve-2024-52911.md)、[不变式 43](../invariants/README.md#43-后台验签必须先于预计算数据结束)。

## 一句话

另一项检查失败就 return，文案写成「这块已经拒了，并行脚本检查不会再碰预计算」。

## 正确写法

「合法路径会 Wait()；提前 return 必须同样汇合队列。作业若只握指针，数据不得先于作业死。」
