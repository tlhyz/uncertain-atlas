# 模式：点名 ewbcast-notpre 杠

**层次**：实现 / ExtWhenBcast broadcast not already construct-Precommit / not already 438-bundled / not already return-ext 正式三事（513 余量）。  
**分类**：建议（产品）。  
**来源**：CometBFT 官方 [ABCI++ Methods](https://github.com/cometbft/cometbft/blob/main/spec/abci/abci++_methods.md) ExtendVote When step 7 broadcasts the Precommit message 句。  
**对应**：[`../tracks/implementation/worked-example-ewbcast-notpre-vs-bundled.md`](../tracks/implementation/worked-example-ewbcast-notpre-vs-bundled.md)。

- **广播 Precommit 不是已经 constructs Precommit 不是已经 constructs Precommit bundled：看见广播 Precommit 不是已经 constructs Precommit，不是已经 constructs Precommit bundled interchangeable / 1343 ewbcast-notpre interchangeable。**
- **broadcasts Precommit is not construct Precommit 不是已经 ExtendVote When 正式流程 bundled：看见broadcasts Precommit is not construct Precommit，不是已经 ExtendVote When 正式流程 bundled interchangeable / 1343 ewbcast-notpre interchangeable。**
- **广播 Precommit 不是已经 constructs Precommit 不是已经 return extension bundled：看见广播 Precommit 不是已经 constructs Precommit，不是已经 return extension bundled interchangeable / 1343 ewbcast-notpre interchangeable。**

**建议（产品，不是事实）**：不确定第一条结算机如果给人看 ExtendVote When broadcast Precommit 正式三事（513 余量），必须分开 not already construct-Precommit、not already step7-order、not already last_commit 三件事。
