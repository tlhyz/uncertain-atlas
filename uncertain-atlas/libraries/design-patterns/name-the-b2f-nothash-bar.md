# 模式：点名 b2f-nothash 杠

**层次**：实现 / EIP-152 F not already BLAKE2b-hash / not already keccak / not already 230-bundled 正式三事（230 余量）。  
**分类**：建议（产品）。  
**来源**：Ethereum [EIP-152](https://eips.ethereum.org/EIPS/eip-152)（Final, Core, Add BLAKE2 compression function F precompile）。  
**对应**：[`../tracks/implementation/worked-example-b2f-nothash-vs-bundled.md`](../tracks/implementation/worked-example-b2f-nothash-vs-bundled.md)。

- **BLAKE2 压缩函数 F 不是已经是 BLAKE2b 哈希 不是已经是 BLAKE2b 哈希：看见BLAKE2 压缩函数 F 不是已经是 BLAKE2b 哈希，不是已经是 BLAKE2b 哈希 interchangeable / 1434 b2f-nothash interchangeable。**
- **F is not already BLAKE2b hash 不是已经是 keccak / SHA3：看见F is not already BLAKE2b hash，不是已经是 keccak / SHA3 interchangeable / 1434 b2f-nothash interchangeable。**
- **BLAKE2 压缩函数 F 不是已经是 BLAKE2b 哈希 不是已经 230 bundled：看见BLAKE2 压缩函数 F 不是已经是 BLAKE2b 哈希，不是已经 230 bundled interchangeable / 1434 b2f-nothash interchangeable。**

**建议（产品，不是事实）**：不确定第一条结算机如果给人看 EIP-152 BLAKE2F 正式三事（230 余量），必须分开 not already hash、not already Equihash-live、not already generic-hash-API 三件事。
