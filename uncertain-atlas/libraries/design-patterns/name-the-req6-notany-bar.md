# 模式：点名 正确进程交出的扩展必须被正确接收者 Verify Accept not already any extension Accepts / not already default Accept / not already settled 正式三事（348 余量）

**层次**：实现 / Extend–Verify 一致性。  
**分类**：建议（产品）。  
**对应例**：[worked-example-req6-notany-vs-bundled.md](../../tracks/implementation/worked-example-req6-notany-vs-bundled.md)。

正确进程交出的扩展必须被正确接收者 Verify Accept not already any extension Accepts / not already default Accept / not already settled 正式三事（348 余量） 要先数清问的是哪一件，再决定要不要同一次发布。

- **必须过 不是已经任意扩展都会 Accept：** 看见正确进程之间永远过，不是已经是任意扩展都会 Accept interchangeable / 869 req6-notany interchangeable。
- **看见必须 Accept 不是已经写了默认 Accept：** 看见必须 Accept，不是已经写了默认 Accept interchangeable。
- **看见正确进程之间过 不是已经交差：** 看见正确进程之间过，不是已经交差 interchangeable。

**建议（产品，不是事实）**：不确定第一条结算机如果给人看正确进程交出的扩展必须被正确接收者 Verify Accept 正式三事（348 余量），先数清问的是是不是已经是任意扩展都会 Accept、是不是已经写了默认 Accept、还是看见正确进程之间过是不是已经交差，再决定要不要同一次发布。348 req6 vs accept bundled unbundling 在本页 item 1 启动。
