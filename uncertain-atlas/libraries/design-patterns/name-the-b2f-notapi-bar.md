# 模式：点名 b2f-notapi 杠

**层次**：实现 / EIP-152 fixed-input not already generic-hash-API / not already digest / not already 199 正式三事（230 余量）。  
**分类**：建议（产品）。  
**来源**：Ethereum [EIP-152](https://eips.ethereum.org/EIPS/eip-152)（Final, Core, Add BLAKE2 compression function F precompile）。  
**对应**：[`../tracks/implementation/worked-example-b2f-notapi-vs-bundled.md`](../tracks/implementation/worked-example-b2f-notapi-vs-bundled.md)。

- **定长输入不是已经是任意哈希 API 不是已经是任意哈希 API：看见定长输入不是已经是任意哈希 API，不是已经是任意哈希 API interchangeable / 1436 b2f-notapi interchangeable。**
- **fixed-length input is not already a generic hash API 不是已经返回一份摘要：看见fixed-length input is not already a generic hash API，不是已经返回一份摘要 interchangeable / 1436 b2f-notapi interchangeable。**
- **定长输入不是已经是任意哈希 API 不是已经是不变量 199：看见定长输入不是已经是任意哈希 API，不是已经是不变量 199 interchangeable / 1436 b2f-notapi interchangeable。**

**建议（产品，不是事实）**：不确定第一条结算机如果给人看 EIP-152 BLAKE2F 正式三事（230 余量），必须分开 not already hash、not already Equihash-live、not already generic-hash-API 三件事。
