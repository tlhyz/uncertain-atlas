# 模式：点名 purp43-notbtc 杠

**层次**：应用 / BIP-43 same-xpub-prefix not already bitcoin-only / not already BIP32-default-account / not already settled 正式三事（266 余量）。  
**分类**：建议（产品）。  
**来源**：Bitcoin [BIP-43](https://github.com/bitcoin/bips/blob/master/bip-0043.mediawiki)（Deployed, Applications）。  
**对应**：[`../tracks/implementation/worked-example-purp43-notbtc-vs-bundled.md`](../tracks/implementation/worked-example-purp43-notbtc-vs-bundled.md)。

- **同一套扩展钥前缀 不是已经是比特币专用：** 看见还是那套前缀，不是已经是比特币专用 interchangeable / 1120 purp43-notbtc interchangeable。
- **看见默认账户支 不是已经是 BIP32 默认账户：** 看见默认账户支，不是已经是 BIP32 默认账户 interchangeable。
- **看见不必另开魔数 不是已经交差：** 看见不必另开魔数，不是已经交差 interchangeable。

**建议（产品，不是事实）**：不确定第一条结算机如果给人看同一套扩展钥前缀 正式三事（266 余量），先数清问的是是不是已经是比特币专用、是不是已经是 BIP32 默认账户、还是看见不必另开魔数是不是已经交差，再决定要不要同一次发布。266 purpose vs compatible bundled unbundling 在本页 item 3 完成。
