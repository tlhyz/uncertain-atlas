# 模式：点名 stcall-notchg 杠

**层次**：实现 / EIP-214 static-state-change not already applied / not already CALLCODE-value / not already 140 正式三事（178 余量）。  
**分类**：建议（产品）。  
**来源**：Ethereum [EIP-214](https://eips.ethereum.org/EIPS/eip-214)（Final, Core, STATICCALL）。  
**对应**：[`../tracks/implementation/worked-example-stcall-notchg-vs-bundled.md`](../tracks/implementation/worked-example-stcall-notchg-vs-bundled.md)。

- **静态帧里改状态不是已经改成 不是已经改成：看见静态帧里改状态不是已经改成，不是已经改成 interchangeable / 1385 stcall-notchg interchangeable。**
- **static state change is not already applied 不是已经和 CALLCODE 带值同一盏灯：看见static state change is not already applied，不是已经和 CALLCODE 带值同一盏灯 interchangeable / 1385 stcall-notchg interchangeable。**
- **静态帧里改状态不是已经改成 不是已经是 140 回滚：看见静态帧里改状态不是已经改成，不是已经是 140 回滚 interchangeable / 1385 stcall-notchg interchangeable。**

**建议（产品，不是事实）**：不确定第一条结算机如果给人看 EIP-7 DELEGATECALL 正式三事（178 余量），必须分开 not already CALLCODE、not already CALL、not already 7702 三件事。
