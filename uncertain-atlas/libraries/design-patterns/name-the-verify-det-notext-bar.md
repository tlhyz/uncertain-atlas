# 模式：点名 Verify 必须只依赖扩展、这块和上一份状态 not already ExtendVote-style other values / not already same ruler as ExtendVote / not already settled 正式三事（341 余量）

**层次**：实现 / VerifyVoteExtension 确定性。  
**分类**：建议（产品）。  
**对应例**：[worked-example-verify-det-notext-vs-bundled.md](../../tracks/implementation/worked-example-verify-det-notext-vs-bundled.md)。

Verify 必须只依赖扩展、这块和上一份状态 not already ExtendVote-style other values / not already same ruler as ExtendVote / not already settled 正式三事（341 余量） 要先数清问的是哪一件，再决定要不要同一次发布。

- **必须确定 不是已经可以像 ExtendVote 那样依赖其它值：** 看见必须确定，不是已经可以像 ExtendVote 那样依赖其它值 interchangeable / 890 verify-det-notext interchangeable。
- **看见只依赖扩展、这块和上一份状态 不是已经和 ExtendVote 同一把尺：** 看见只依赖扩展、这块和上一份状态，不是已经和 ExtendVote 同一把尺 interchangeable。
- **看见 Verify 回了 不是已经交差：** 看见 Verify 回了，不是已经交差 interchangeable。

**建议（产品，不是事实）**：不确定第一条结算机如果给人看 Verify 必须只依赖扩展、这块和上一份状态 正式三事（341 余量），先数清问的是是不是已经可以像 ExtendVote 那样依赖其它值、是不是已经和 ExtendVote 同一把尺、还是看见 Verify 回了是不是已经交差，再决定要不要同一次发布。341 verify-det vs extend bundled unbundling 在本页 item 1 启动。
