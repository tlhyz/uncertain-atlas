# 将形成的全部知识资产目录

这些是长期资产。正文按轨追加。  
每一份都必须能回溯到 [`../GOAL.md`](../GOAL.md)。

---

## 课程资产

| 编号 | 资产 | 路径 | 状态 | 优先级 |
|---|---|---|---|---|
| 01 | 《区块链基础课程》 | `courses/` | L0–L10 正文已写；L8.4 数学后置 | 必学 |
| 02 | 《全球公链架构图谱》 | `protocols/` 19 节 + `tracks/` 横表 | 主线 + 第 8 波择优（含 Nervos 思想级） | 必学 |
| 03 | 《共识协议图谱》 | `tracks/consensus/` + `tracks/finality/` | 对照表 + ABCI 四门 + 扩展 + 集合延迟 + PBTS + Bitcoin MTP 三把尺 + 本地超时 ≠ 最终性 + 应用 delay ≠ 槽位 + 默认 MaxBytes ≠ 第一轮 SLA + backed ≠ 可用 + BABE ≠ GRANDPA + head ≠ justified ≠ finalized + NPoS 等权 ≠ 按质押凑 ⅔ + leak ≠ slash + 抽样 α ≠ QC + 已认证批次 ≠ L + PoH ≠ Tower + VRF 抽中 ≠ 已认证 + Doomslug ≠ BFT 谓词 + 顺序 ≠ 状态根 + DAG ≠ selected chain + L2 档 ≠ L1 + unsafe ≠ 已推导 + 本头 AppHash ≠ 本块已交差 + 本头 LastCommit ≠ 本高已 +2/3 + 处理完一块 ≠ 已经改规范头 + 父信标根 ≠ 当前头 + 请求承诺 ≠ 已经由共识层处理完 + 执行层退出请求 ≠ 已经退出 + 执行层存款日志 ≠ 已经办完 + 历史执行哈希 ≠ BLOCKHASH + 合并请求 ≠ 已经并成一把 + 委员会下标被挪出签名 ≠ 已经没有委员会；看见 RANDAO 种子已知 ≠ 已经锁死出块日程（不变量 205）；看见签过的自愿退出 ≠ 已经永远有效（不变量 213）；看见加长证明纳入窗 ≠ 已经有确认规则（不变量 214）；看见激活流失帽 ≠ 已经改了奖励曲线（不变量 215）；看见难度把叔块算进去 ≠ 已经按个数调 / 已经没有炸弹（不变量 238）；看见同一交易标识 ≠ 已经唯一 / 许多确认 ≠ 已经不怕被覆盖（不变量 257）；看见多余栈元素 ≠ 已经随便填 / 隔离见证 ≠ 已经没有这条延展（不变量 264） | 必学 |
| 04 | 《状态模型图谱》 | `tracks/state-models/` + `tracks/parallelism/` | 进行中；Block-STM 工作实例（不变量 122）；owned ≠ 快路径（不变量 128）；Quorum Store ≠ 排序（不变量 132）；顺序 ≠ 状态根（不变量 136）；谓词 ≠ 脚本 / 只读重叠 ≠ 写冲突（不变量 143）；引用输入 ≠ 已经花费（不变量 150）；`store` ≠ 顶层资源（不变量 151）；瞬时存储 ≠ 账户持久存储（不变量 159）；后来的 SELFDESTRUCT ≠ 账户已经删掉（不变量 160）；发送者已有代码 ≠ 已经能当 EOA 发交易（不变量 162）；脚本里的 CLTV ≠ 交易 nLockTime 已经把输出锁住（不变量 164）；脚本里的 CSV ≠ 绝对锁 / ≠ 部署名（不变量 165）；付给脚本哈希 ≠ 已经揭开赎回脚本（不变量 170）；nonce 顶到规范上限 ≠ 已经还能加一（不变量 175）；空 ≠ 已经不存在（不变量 180）；看见授权名单 ≠ 已经委托成功（不变量 190）；看见代码哈希指令 ≠ 已经看见代码本身（不变量 221）；看见盐创建指令 ≠ 已经是按序号占址 / 算出来的盐地址 ≠ 已经创建（不变量 222）；看见弃用警告 ≠ 已经改了共识行为（不变量 224） | 必学 |
| 05 | 《区块链密码学地图》 | `tracks/crypto/` | 对照表 + 域分离三层编码精读；txid ≠ wtxid（不变量 152）；PREVRANDAO ≠ 工作量 / 无偏骰子（不变量 157）；JSON chainId ≠ 已经编进签名哈希（不变量 161）；ECDSA 验得过 ≠ 已经是严格 DER（不变量 172）；看见 BLS12-381 预编译 ≠ 已经在验 BLS 签（不变量 199）；看见 P256 验签预编译 ≠ 已经在验 k1（不变量 204）；看见签过的自愿退出 ≠ 已经永远有效（不变量 213）；看见链号指令 ≠ 已经是签进哈希的链号（不变量 220）；看见签过的消息 ≠ 已经控制资金（不变量 258）；本页这种签消息 ≠ 已经是 322 / 头字节标了种类 ≠ 已经有地址 / 旧 P2PKH 习惯 ≠ 已经互操作（不变量 294）；看见多余栈元素 ≠ 已经随便填（不变量 264） | 必学 |
| 05b | 《实现保证精读》 | `tracks/implementation/` | 编码 + 崩溃 + assumevalid + 头工作量 + state sync + 平台宽度尺寸检查；txid ≠ wtxid（不变量 152）；钥匙路径 ≠ 揭树（不变量 153）；类型信封 ≠ 已经解开内层（不变量 167）；列入访问列表 ≠ 已经访问过（不变量 168）；本笔第一次碰 ≠ 已经热（不变量 169）；版本位被置上 ≠ 已经锁定 / 锁定 ≠ 已经激活（不变量 171）；ECDSA 验得过 ≠ 已经是严格 DER（不变量 172）；coinbase 高度 ≠ 头上已经有高度字段（不变量 173）；看见 Bech32 地址串 ≠ 链上已经有这笔输出（不变量 174）；initcode 超界 ≠ 已经是部署代码超界（不变量 176）；带回剩余气的回滚 ≠ 已经烧光剩余气（不变量 177）；静态帧 ≠ 已经是高级语言只读（不变量 178）；看见部分签名包 ≠ 已经能广播（不变量 179）；后继校验过了 ≠ 已经是旧校验那套地址（不变量 181）；看见扩展公钥 ≠ 已经能花（不变量 182）；看见助记词 ≠ 已经是二进制种子（不变量 183）；看见私钥或助记词备份 ≠ 已经知道该看哪种输出脚本（不变量 184）；创建结束返回的运行时代码超界 ≠ 已经是 initcode 超界（不变量 185）；出块者地址开跑时已在热集合 ≠ 已经访问过（不变量 187）；新创建要存上链的代码以保留首字节开头 ≠ 已经是对象格式已经部署（不变量 188）；看见后继版本工作包 ≠ 已经是旧版那份固定未签交易（不变量 186）；走脚本路径 ≠ 已经是 tapscript 语义（不变量 189）；看见授权名单 ≠ 已经委托成功（不变量 190）；看见 Miniscript ≠ 已经是链上脚本（不变量 191）；看见头里的请求承诺 ≠ 已经由共识层处理完（不变量 192）；看见状态里的历史执行哈希 ≠ 已经是 BLOCKHASH（不变量 195）；看见 calldata 地板 ≠ 已经改了执行气（不变量 197）；看见 BLS12-381 预编译 ≠ 已经在验 BLS 签（不变量 199）；看见 RLP 编码硬帽 ≠ 已经改了气限（不变量 202）；看见单笔气帽 ≠ 已经改了块气限（不变量 203）；看见 P256 验签预编译 ≠ 已经在验 k1（不变量 204）；看见 MODEXP 输入长度帽 ≠ 已经改了计价（不变量 206）；看见对等节点宣布历史窗 ≠ 已经改了共识历史（不变量 207）；看见数前导零操作码 ≠ 已经更便宜的 ZK 证明（不变量 208）；看见分叉配置 RPC 对上了 ≠ 已经过多客户端同根（不变量 210）；看见客户端默认气限 ≠ 已经是协议帽（不变量 211）；看见块级访问名单 ≠ 已经并行跑完（不变量 212）；看见内存拷贝指令 ≠ 已经是身份预编译（不变量 216）；看见压零指令 ≠ 已经是带立即数的压 0（不变量 217）；看见基础费指令 ≠ 已经改了费用市场（不变量 218）；看见 blob 基础费指令 ≠ 已经是执行层基础费指令（不变量 219）；看见链号指令 ≠ 已经是签进哈希的链号（不变量 220）；看见代码哈希指令 ≠ 已经看见代码本身（不变量 221）；看见盐创建指令 ≠ 已经是按序号占址 / 算出来的盐地址 ≠ 已经创建（不变量 222）；看见退款削减 ≠ 已经没有退款（不变量 223）；看见弃用警告 ≠ 已经改了共识行为（不变量 224）；看见净计量 ≠ 已经是瞬时存储（不变量 225）；看见非零 calldata 降价 ≠ 已经没有块大小上限（不变量 226）；看见模幂重计价 ≠ 已经是 198 那道复杂度公式（不变量 227）；看见 bn128 预编译降价 ≠ 已经在验签（不变量 228）；看见本账户余额指令 ≠ 已经是按地址查余额（不变量 229）；看见 BLAKE2 压缩函数 F ≠ 已经是哈希（不变量 230）；看见原生移位指令 ≠ 已经用算术拼过移位（不变量 231）；看见返回数据缓冲 ≠ 已经是内存（不变量 232）；看见委托调用 ≠ 已经是 CALLCODE（不变量 233）；看见 Homestead ≠ 已经改了 CREATE / 已经拒高 s / 已经限制代码 / 已经没有炸弹（不变量 234）；看见收据状态码 ≠ 已经能从剩余气推断成功（不变量 236）；看见读树涨价 ≠ 已经换成去掉六十四分之一 / 问超了 ≠ 已经耗尽气（不变量 237）；看见多余栈元素 ≠ 已经随便填 / 隔离见证 ≠ 已经没有这条延展（不变量 264）；看见 signet ≠ 已经是 testnet / 已经是 regtest / 头上工作量 ≠ 已经签过（不变量 265）；看见 BIP32 compatible ≠ 已经能互操作 / 自称 BIPxx compatible ≠ 已经是那份结构（不变量 266）；看见同一份种子 ≠ 已经是同一条币 / 余额为零 ≠ 已经发现完（不变量 267）；看见同一套 BIP44 账户 ≠ 已经能找回嵌套隔离见证 / 账户出现了 ≠ 已经不用核余额（不变量 268）；看见脚本类型层 ≠ 已经是账户层 / 现有多签习惯 ≠ 已经要搬家（不变量 269）；看见同一套钥 ≠ 已经是同一条 P2SH 地址 / 未压缩钥 ≠ 已经是本页（不变量 270）；看见共享主公钥 ≠ 已经是本页 / 前面分支没有交易 ≠ 已经发现完（不变量 271）；看见派生钥 ≠ 已经是输出钥 / 不需要脚本路径 ≠ 已经不承诺（不变量 272）；看见付款码 ≠ 已经是存款地址 / 通知输出 ≠ 已经能花（不变量 273）；看见 multi ≠ 已经按字典序排 / 门限 ≠ 已经同一套上限（不变量 274）；看见 tr 没有树 ≠ 已经有脚本路径 / 压缩钥 ≠ 已经是 x-only（不变量 275）；看见 pk ≠ 已经和 pkh / sh 同一套放置 / 熟悉脚本 ≠ 已经能互操作（不变量 276）；看见 wpkh / wsh ≠ 已经只能顶层 / 未压缩钥 ≠ 已经允许（不变量 277）；看见 multi_a ≠ 已经是 383 那种 multi / 门限 ≠ 已经同一套编码（不变量 278）；看见旧 PSBT 栏 ≠ 已经能装 Taproot / 输出脚本里的钥 ≠ 已经是内部钥（不变量 279）；看见钱包策略 ≠ 已经是一条描述符 / 登记过 ≠ 已经批准这笔花（不变量 280）；看见 combo ≠ 已经只能产出一种脚本 / 未压缩钥 ≠ 已经带齐见证对（不变量 281）；看见 raw ≠ 已经能套进具名表达式 / addr ≠ 已经是那份输出脚本（不变量 282）；看见聚合钥 ≠ 已经是扩展公钥 / 合成扩展公钥 ≠ 已经能硬化派生（不变量 283）；看见旧 PSBT 栏 ≠ 已经能装 MuSig2 / 聚合钥栏 ≠ 已经是输出钥（不变量 284）；看见脚本各走各的路径 ≠ 已经是多签该有的树 / 路径里的脚本类型 ≠ 已经必要（不变量 285）；看见一份助记词 ≠ 已经能备齐所有钱包 / 派生出的熵 ≠ 已经是目标钱包的种子（不变量 286）；看见部分签名包 ≠ 已经是跨厂安全多签开户 / TOKEN ≠ 已经是钱包种子（不变量 287）；看见一条派生路径 ≠ 已经是一份路径模板 / 写死熟路径 ≠ 已经能互操作（不变量 288）；看见共享了扩展公钥 ≠ 已经是链码委托 / 本笔微调 ≠ 已经是盲签（不变量 289）；带 pj= 的付款 URI ≠ 已经是 payjoin 付款 / 原始包 ≠ 已经是提案 / 收款方加了输入 ≠ 已经另开一笔（不变量 290）；自家习惯的输入输出顺序 ≠ 已经是字典序标准 / 按字典序排了 ≠ 已经是共识 / ≠ 已经私人（不变量 291）；Testnet 4 ≠ 已经是 Testnet 3 / 20 分钟例外 ≠ 已经没有块风暴 / 会 Testnet 3 ≠ 已经能安全跟（不变量 292）；储备证明交易 ≠ 已经能花 / 其余输入签过 ≠ 已经控制资金 / POR 栏 ≠ 已经是普通花费（不变量 293）；加密私钥记录 ≠ 已经能用 / 厂家代生成 ≠ 已经能兑 / 地址哈希片段 ≠ 已经是地址（不变量 296）；本页这种地址 ≠ 已经是赎回脚本 / 旧软件报无效 ≠ 已经付过 / 只有地址 ≠ 已经知道付给谁（不变量 297）；写下每条消息 ≠ 已经 fsync / 回放时又要签 ≠ 已经双签 / LastSignBytes 对上 ≠ 已经换了高度（不变量 298）；先装证据 ≠ 已经装满交易 / 两条收交易上限 ≠ 已经同一条 / MaxBytes 写成 -1 ≠ 已经没有上限（不变量 299）；本地 State ≠ 已经进了块 / 头上的根 ≠ 已经有了 State / 能读本地 State ≠ 已经进了规范（不变量 300）；提案收了 ≠ 已经从池里删掉 / commit 后 ≠ 已经不用再验剩下的 / CheckTx 过了 ≠ 已经永远有效（不变量 301）；同一高度换轮 ≠ 已经换了集合 / 新加入 ≠ 已经能跳到队头 / 优先级差被缩放 ≠ 已经按人头轮（不变量 302）；创世 app_state ≠ 已经验过应用状态 / 进程起来 ≠ 已经过了 genesis_time / 空 validators ≠ 已经没有集合（不变量 303）；票上 Timestamp ≠ 已经验过 / 冲突提案 ≠ 已经有证据 / 非法被断开 ≠ 已经罚了（不变量 304）；InitPeer ≠ 已经能交互 / Receive ≠ 已经 AddPeer / 跑着 ≠ 已经能再登记（不变量 305）；Peer 句柄 ≠ 已经是那个人 / Broadcast 回了通道 ≠ 已经送到 / StopPeerForError ≠ 已经对持久邻居也断干净（不变量 306）；同进程 ≠ 已经隔离 / gRPC 最容易 ≠ 已经快 / 一条连接 ≠ 已经是四门（不变量 307）；NumPeers ≠ 已经数完 / 按名字拿到反应堆 ≠ 已经独立 / PeerState ≠ 已经验过高度（不变量 308）；HasChannel ≠ 已经入队 / Send 回假 ≠ 已经断开 / TrySend ≠ 已经和 Send 同一把尺（不变量 309）；默认锁 ≠ 已经 RPC 安全 / Commit 前上锁 ≠ 已经解锁 / Commit 里等广播 ≠ 已经能往下走（不变量 310）；Prepare 没有头哈希 ≠ 已经知道本头 / 候选 ≠ 已经是 ExecuteTxState / 丢掉 ≠ 已经永远不用再执行（不变量 311）；CheckTx 过了 ≠ 已经按 ExecuteTxState 验过 / 两份同时在改 ≠ 已经同一份 / RECHECK ≠ 已经是新交易（不变量 312）；内存池去重 ≠ 已经保证不重放 / 过了 CheckTx ≠ 已经有应用级保护 / 通常不受欢迎 ≠ 已经没有幂等例外（不变量 313）；QueryState ≠ 已经是 ExecuteTxState / 上次 Commit ≠ 已经跟上正在跑的块 / 启动对齐 ≠ 已经是快照重放（不变量 314）；MaxGas ≠ 已经在执行 / GasUsed ≠ 已经算进共识 / 已提交块 ≠ 已经按气验过（不变量 315）；结果列表 ≠ 已经同一顺序 / Code 非零 ≠ 已经没进块 / Code Data ≠ 已经印进本头（不变量 316）；CheckTx 的 Data ≠ 已经被引擎用了 / 各节点 Data 不一样 ≠ 已经分叉 / Priority ≠ 已经是共识顺序（不变量 317）；InitChain 空名单 ≠ 已经没有集合 / 同一批重复公钥 ≠ 已经能恢复 / power 0 ≠ 已经删掉不在集合里的人（不变量 318）；InitChain 空参数 ≠ 已经没有参数 / Finalize 没回 ≠ 已经清掉 / 只改一个字段 ≠ 已经只改这一项（不变量 319）；应用高度比引擎高 ≠ 已经允许 / 块进 store ≠ 已经 Commit / 启动 Info 对上 ≠ 已经能跳步（不变量 320）；Offer 收下 ≠ 已经装完 / 一块 chunk 收下 ≠ 已经齐 / 拉失败换一份 ≠ 已经能接着装（不变量 321）；ListSnapshots 回了 ≠ 已经有了全部快照 / 挑了最高 ≠ 已经收下 / Offer 被拒 ≠ 已经停（不变量 322）；装完 ≠ 已经有了 ChainID / AppHash 对上 ≠ 已经版本也对上 / 切进共识 ≠ 已经有完整历史（不变量 323）；拍了这个高度 ≠ 已经交差之后拍的 / 没停链 ≠ 已经一致 / 只留最近两份 ≠ 已经有了全部历史快照（不变量 324）；头上有 AppHash ≠ 已经是交易默克尔 / Query 回了 Proof ≠ 已经对上 AppHash / 一层 ProofOp 的根 ≠ 已经对上最终 AppHash（不变量 325）；发了 addr 过滤查询 ≠ 已经收下这个人 / id 过滤查询绿了 ≠ 已经过了 addr / 有 /store 路径 ≠ 已经是引擎在用（不变量 326）；立刻整块执行 ≠ 已经离开提议超时的关键路径（不变量 327）；同一高度回了不同码 ≠ 已经有了 CheckTxCode（不变量 328）；Query 回了 ≠ 已经复制到各节点（不变量 329）；到了 H ≠ 已经 Prepare 带了扩展（不变量 330）；填了证据 MaxBytes ≠ 已经落在块上限下面（不变量 331）；装完又对上 LastBlockAppHash ≠ 已经在装回当中验过（不变量 332）；本高回了 ConsensusParams ≠ 已经在本高生效（不变量 333）；四门里有 Snapshot Connection ≠ 已经必须实现快照（不变量 334）；Finalize 改了状态 ≠ 已经落盘（不变量 335）；填了 Precision ≠ 已经是 MessageDelay（不变量 336）；-1 就按 100 MB 验 ≠ 已经没有上限（不变量 337）；Prepare 没有确定性要求 ≠ 已经必须确定（不变量 338）；不该验排序相关有效性 ≠ 已经该在 CheckTx 里验（不变量 339）；Process 必须只依赖请求和上一份状态 ≠ 已经可以像 Prepare 那样依赖其它值（不变量 340）；Verify 必须只依赖扩展、这块和上一份状态 ≠ 已经可以像 ExtendVote 那样依赖其它值（不变量 341）；Finalize 算出的状态必须只依赖上一份状态和决定块 ≠ 已经可以像 Prepare 那样依赖其它值（不变量 342）；写成 0 不是已经启用 PBTS ≠ 已经填了 Precision 就是 PBTS（不变量 343）；MaxBytes 减去头集合证据才是交易上限 ≠ 已经整块都能装交易（不变量 344）；整池可见 ≠ 已经只能看见装得进一块的子集（不变量 345）；必须协调升级 ≠ 已经只改 VoteExtensionsEnableHeight（不变量 346）；正确提议者的准备提案必须被正确接收者 Accept ≠ 已经是任意块都会 Accept（不变量 347）；正确进程交出的扩展必须被正确接收者 Verify Accept ≠ 已经是任意扩展都会 Accept（不变量 348）；Prepare 不得改已提交状态 ≠ 已经立刻执行就已经交差（不变量 349）；一轮最多一张 Precommit ≠ 已经能再签一张（不变量 350）；Process 也会在提议者那边叫 ≠ 已经不用再 Process（不变量 351）；+2/3 之后才进来的扩展写进了 commit info ≠ 已经 Verify 过（不变量 352）；空扩展仍会调 Verify ≠ 已经跳过 Verify（不变量 353）；Process 调用是同步的 ≠ 已经能在返回之后再改裁决（不变量 354）；从提案拿掉 tx ≠ 已经从内存池删掉（不变量 355）；validValue 非 nil ≠ 已经还会调 Prepare（不变量 356）；引擎没有再验重复交易 ≠ 已经验过重复（不变量 357）；vote_extension 会包进 CanonicalVoteExtension ≠ 已经按原样签（不变量 358）；Prepare 和 Process / Finalize 同一套字段 ≠ 已经跑过 Process（不变量 359）；至少一名非拜占庭验证者跑过 Process ≠ 已经每个验证者都跑过 Process（不变量 360）；+2/3 prevote 同一 id(v) 才锁住再调 ExtendVote ≠ 已经会调 ExtendVote（不变量 361）；+2/3 precommit 同一 id(v) 才决定再调 Finalize ≠ 已经会调 Finalize（不变量 362）；Finalize 等价于 ABCI 1.0 那三步 ≠ 已经是四门已经结算（不变量 363）；Validator 用 address 认人 ≠ 已经带了公钥（不变量 364）；VoteInfo 能按到场定奖惩 ≠ 已经罚没（不变量 365）；retain_height 默认 0 ≠ 已经在剪（不变量 366）；没定义 lane_priorities ≠ 已经排了优先（不变量 367）；快照全字段（含 Metadata）对上 ≠ 已经装完（不变量 368）；ExtendedVoteInfo 从本进程抽出 ≠ 已经从块里抽出（不变量 369）；Info 用来握手对齐 ≠ 已经是快照重放（不变量 370）；Query 可以对当前或过去高度查 ≠ 已经是 QueryState（不变量 371）；Misbehavior.type 只是过错枚举 ≠ 已经罚没（不变量 372）；CheckTx 技术上可选、不参与处理块 ≠ 已经是四门已经结算（不变量 373）；Flush 要把客户端排队的消息冲到服务端 ≠ 已经送到（不变量 374）；LoadSnapshotChunk 用来从邻居拉快照块 ≠ 已经齐（不变量 375）；UNKNOWN 一律是错、引擎当应用坏了会崩 ≠ 已经是四门已经结算（不变量 376）；data 按 URI 查询分量解释、可以和 path 一起或代替 path 用 ≠ 已经是 Query 高度（不变量 377）；应用可以再拉块或封邻居、引擎不自己做 ≠ 已经封了（不变量 378）；Info 请求 version 是 CometBFT 软件语义版本 ≠ 已经是 app_version（不变量 379）；Query 回包 index 是树里这个键的下标 ≠ 已经是按键查（不变量 380）；CheckTx 回包 codespace 是码的命名空间 ≠ 已经是回包码（不变量 381）；syncing_to_height 同步或重放时是目标高、否则等于本高 ≠ 已经有完整历史（不变量 382）；Query 请求 prove 是能回就回默克尔证明 ≠ 已经对上 AppHash（不变量 383）；Query 回包 code 是回包码 ≠ 已经过了共识（不变量 384）；ConsensusParams.block 限制块大小和块间隔 ≠ 已经是 MaxBytes 上限（不变量 385）；ConsensusParams.evidence 限制拜占庭证据是否合法 ≠ 已经是证据 MaxBytes（不变量 386）；InitChain 请求 time 是创世时间 ≠ 已经过了 genesis_time（不变量 387）；InitChain 请求 consensus_params 是起步共识参数 ≠ 已经没有参数（不变量 388）；Info 回包 data 是任意信息 ≠ 已经是握手对齐（不变量 389）；ProofOp.key 是这棵默克尔树里这把键 ≠ 已经是 Query 回包键（不变量 390）；CheckTx 请求 tx 是请求交易字节 ≠ 已经是 Recheck（不变量 391）；InitChain 回包 app_hash 是起步应用哈希 ≠ 已经是本头 AppHash（不变量 392）；ExecTxResult.gas_wanted 是这笔要的气 ≠ 已经是 CheckTx 的 GasWanted（不变量 393）；ExtendedCommitInfo.round 是提交轮 ≠ 已经是 CommitInfo.round（不变量 394）；ListSnapshots 请求是空请求、向应用要一份快照清单 ≠ 已经齐（不变量 395）；OfferSnapshot 请求 snapshot 是拿来装回的那份快照 ≠ 已经是本地清单（不变量 396）；ApplySnapshotChunk 请求 chunk 是 LoadSnapshotChunk 回的那块二进制内容 ≠ 已经在拉块（不变量 397）；ApplySnapshotChunk Result RETRY 是再装这块、按需配合 RefetchChunks 和 RejectSenders ≠ 已经再拉（不变量 398）；Commit 不带参数 ≠ 已经落盘（不变量 399）；OfferSnapshot Result REJECT_FORMAT 是拒掉这种 format、换一份 ≠ 已经是拒掉这份（不变量 400）；Offer 收下之后才去拉块并装 ≠ 已经装完（不变量 401）；OfferSnapshot Result UNKNOWN 是结果不明、中止全部装回 ≠ 已经崩（不变量 402）；Finalize 之后引擎才落盘各笔输出 / AppHash / ResultsHash ≠ 已经交差（不变量 403）；Finalize 回包 app_hash 可以空或硬编码、但必须确定 ≠ 已经印进本头（不变量 404）；CheckTx 是内存池的守卫、每条节点先跑 CheckTx 才让交易进本地池 ≠ 已经是技术上可选（不变量 405）；Snapshot.height 是拍快照的高度（Commit 之后） ≠ 已经是 Query 高度（不变量 406）；Finalize 含刚决定那块的字段 ≠ 已经是四门已经结算（不变量 407）；Finalize 按应用自己的规则确定地执行 txs、再交还控制权 ≠ 已经交差（不变量 408）；ExtendVoteRequest 的内容对应共识即将发 Precommit 的那份拟议块 ≠ 已经会调 ExtendVote（不变量 409）；ExtendVoteRequest.hash 是扩展要指的那份拟议块头哈希 ≠ 已经跑过 Process（不变量 410）；ExtendVoteRequest.txs 是扩展要指的那份块的交易列表 ≠ 已经执行那些交易（不变量 411）；InitChain 创世时只调一次 ≠ 已经是崩溃后再调（不变量 412）；ExtendVoteRequest.misbehavior 是拟议块里那些过错信息 ≠ 已经定奖惩（不变量 413）；ExecTxResult.log 是应用日志的输出 ≠ 已经是 Query 日志（不变量 414）；VerifyVoteExtensionRequest.height 是块高度（用来对一下） ≠ 已经是拟议块高度（不变量 415）；进了这一轮会先设 ProposeTimeout ≠ 已经填了 TimeoutPropose（不变量 416）；自己是提议者会先走完 Prepare 那五步 ≠ 已经不用再 Process（不变量 417）；ExtendVoteResponse.vote_extension 是 CometBFT 签的信息、可以 0 长、标成非确定 ≠ 已经会包进 CanonicalVoteExtension（不变量 418）；ProcessProposalRequest.txs 是拟议块的交易列表 ≠ 已经执行那些交易（不变量 419）；ProcessProposalRequest.proposed_last_commit 是从拟议块里的信息拿到的上一份提交信息 ≠ 已经交差 local_last_commit（不变量 420）；ExtendedVoteInfo.vote_extension 是发送验证者的应用给的非确定扩展 ≠ 已经从本进程抽出（不变量 421）；FinalizeBlockRequest.decided_last_commit 是从刚决定那块拿到的上一份提交信息 ≠ 已经交差 local_last_commit（不变量 422）；PrepareProposalRequest.max_tx_bytes 是当前配置的、改过的交易占的最大字节 ≠ 已经能回超限列表（不变量 423）；PrepareProposalRequest.local_last_commit 是从本进程 CometBFT 数据结构拿到的上一份提交信息 ≠ 已经交差 proposed_last_commit（不变量 424）；ExtendedVoteInfo.validator 是发了这张票的验证者 ≠ 已经带了公钥（不变量 425）；PrepareProposalRequest.next_validators_hash 是下一验证者集合默克尔根 ≠ 已经是 Finalize 请求栏的 next_validators_hash（不变量 426）；ProcessProposalRequest.next_validators_hash 是下一验证者集合默克尔根 ≠ 已经是 Prepare 请求末栏的 next_validators_hash（不变量 427）；FinalizeBlockRequest.hash 是已决块的哈希 ≠ 已经是 ProcessProposalRequest.hash（不变量 428）；FinalizeBlockRequest.proposer_address 是造了这份提案的验证者地址 ≠ 已经正在造这份提案（不变量 429）；ProcessProposalResponse.status 是应用认为这份提案合法还是非法 ≠ 已经当成块非法（不变量 430）；FinalizeBlockResponse.events 是给索引用的类型键值事件 ≠ 已经印进本头（不变量 431）；FinalizeBlockResponse.consensus_param_updates 是对 gas、大小和其它共识相关参数的改动 ≠ 已经在块 H 生效（不变量 432）；VerifyVoteExtensionResponse.status 是应用认为这份扩展合法还是非法 ≠ 已经当成块非法（不变量 433）；VerifyStatus 的 UNKNOWN 一律是错、引擎当应用坏了会崩 ≠ 已经验过扩展（不变量 434）；Precommit 没有带有效签的扩展就会当非法丢掉、不调 Verify ≠ 已经跳过 Verify（不变量 435）；VerifyVoteExtensionRequest.validator_address 是签了这份扩展的验证者地址 ≠ 已经带了公钥（不变量 436）；ExtendVoteResponse.vote_extension 只会挂在非 nil Precommit 上、precommit nil 不会叫 ExtendVote ≠ 已经会调 ExtendVote（不变量 437）；应用回 extension 后 CometBFT 会填进 CanonicalVoteExtension、填其它字段并签名 ≠ 已经按原样签（不变量 438）；FinalizeBlock 套用候选 ≠ bundled（不变量 460）；executes txs ≠ committed / apply candidate ≠ ExecuteTxState / previously executed ≠ no re-execute in Finalize / fincand bundled。executes txs deterministically not like Prepare 正式三事（470 余量）≠ bundled（不变量 579）；executes txs deterministically ≠ like Prepare / 470 findet item 1。app_hash MUST be deterministic not 印进本头 正式三事（470 余量）≠ bundled（不变量 580）；app_hash MUST be deterministic ≠ 印进本头 / 470 findet item 2。implementation MUST be deterministic not Req 11–12 正式三事（470 余量）≠ bundled（不变量 581）；implementation MUST be deterministic ≠ Req 11–12 / 470 findet item 3。FinalizeBlock Usage determinism 正式三事 ≠ bundled（不变量 470）；executes txs deterministically / app_hash MUST be deterministic / implementation MUST be deterministic ≠ 已经可以像 Prepare 那样 / findet bundled。FinalizeBlockResponse consensus_param_updates H→H+1 正式三事（不变量 471）；returned for block H apply to H+1 ≠ 已经在块 H 生效 / validator_updates H+2 才计票 / 只填一项 / 清掉参数。Contains newly decided block fields 不是已经是四门已经结算 / 不是 proposed block / 不是 height/time match header 就代表对象已经分清（不变量 474）。optional Merkle root 不是已经是本头 AppHash / 已经印进本头；included as Header.AppHash in the next block 不是已经写进下一块头；Query proofs anchored 不是已经对上 AppHash / 已经是按键查（不变量 475）。executes txs deterministically not already committed 正式三事（460 余量）≠ bundled（不变量 576）；executes txs deterministically ≠ committed / 460 fincand item 1。apply candidate state not ExecuteTxState 正式三事（460 余量）≠ bundled（不变量 577）；apply candidate state ≠ ExecuteTxState / Process ACCEPT switched / 460 fincand item 2。previously executed not no re-execute in Finalize 正式三事（460 余量）≠ bundled（不变量 578）；previously executed ≠ no re-execute txs / no return app_hash / guarantee satisfied / 460 fincand item 3。FinalizeBlock When Application executes block v not +2/3 precommit decided / ResultHash 正式三事（466 余量）≠ bundled（不变量 575）；Application executes block v ≠ +2/3 precommit decided / ResultHash / 466 finexecbv item 3。FinalizeBlock When Application executes block v not apply candidate / Process already ran 正式三事（466 余量）≠ bundled（不变量 574）；Application executes block v ≠ Process already ran / apply candidate / 466 finexecbv item 2。FinalizeBlock When Application executes block v not persist decision / When calling guarantee 正式三事（466 余量）≠ bundled（不变量 573）；Application executes block v ≠ persist decision / When calling guarantee / 466 finexecbv item 1。FinalizeBlock When Application executes block v ≠ bundled（不变量 466）；Application executes block v ≠ persist decision / When calling guarantee / 466 finexecbv parent bundled 启动。FinalizeBlock When calling ProcessProposal guarantee not executes block v / persist decision 正式三事（472 余量）≠ bundled（不变量 572）；When calling guarantee ≠ 已经 persist decision / 466 executes block v。FinalizeBlock When calling ProcessProposal guarantee not proposer means everyone Processed 正式三事（472 余量）≠ bundled（不变量 571）；When calling guarantee ≠ 已经 proposer means everyone Processed / 351 Process also on proposer。FinalizeBlock When calling ProcessProposal guarantee not every validator 正式三事（472 余量）≠ bundled（不变量 570）；When calling guarantee ≠ 已经 472 finwhen bundled / 已经 360 bundled。FinalizeBlock When calling ProcessProposal guarantee ≠ bundled（不变量 472）；When calling guarantee ≠ 已经每个验证者都跑过 Process / 已经 persist decision。FinalizeBlock fill all fields even if Prepare/Process passed ≠ bundled（不变量 473）；even if Prepare/Process passed ≠ field names match means ran Process 正式三事（473 余量）≠ bundled（不变量 563）；will fill up all fields ≠ no need to provide again 正式三事（473 余量）≠ bundled（不变量 565）；tx_results Code==0 完全合法正式三事 ≠ 已经 CheckTx 过了（不变量 585）；misbehavior not VoteInfo availability 正式三事（463 余量）≠ bundled（不变量 569）；decided_last_commit from decided block not proposed_last_commit 正式三事（463 余量）≠ bundled（不变量 568）；can use decided_last_commit + misbehavior to determine rewards not already slashed 正式三事（463 余量）≠ bundled（不变量 567）；all fields / request complete ≠ committed 正式三事（473 余量）≠ bundled（不变量 566）；at least one non-byzantine ran Process not every validator 正式三事（360 余量）≠ bundled（不变量 582）；Finalize 请求把字段再填一遍 not no need to provide again 正式三事（360 余量）≠ bundled（不变量 583）；apply candidate state not ExecuteTxState 正式三事（360 余量）≠ bundled（不变量 584）；VerifyVoteExtension Response status valid/invalid / REJECT rejects whole vote not block invalid / REJECT not can't receive precommit ≠ bundled（不变量 539）；ProcessProposal Response status must exclusively depend / exclusive dependence not Prepare nondet / status exclusive dependence not Process 340 same ruling ≠ bundled（不变量 538）；ProcessProposal Response status valid/invalid / REJECT assumes not valid not block invalid / REJECT not can't execute candidate ≠ bundled（不变量 537）；ProcessProposal REJECT consensus assume not can't execute candidate / candidate state not committed / not already settled ≠ bundled（不变量 536）；ProcessProposal REJECT prevote nil not Verify whole vote / not Process resp bundled / not async can still Reject ≠ bundled（不变量 535）；ProcessProposal REJECT consensus assumes not valid not block invalid / not permanently blacklisted / not ProposalStatus REJECT ≠ bundled（不变量 534）；ProcessProposal REJECT 共识假设 / prevote nil / assumes not valid ≠ 已经当成块非法 / 已经 Verify 拒整张票 / 已经不能整块执行候选（不变量 533）；FinalizeBlock must provide values not already changed set / settled 正式三事（477 余量）≠ bundled（不变量 594）；must provide 四列 not changed set / not settled / not finasresult bundled / 477 finasresult item 1；FinalizeBlock must provide values as a result of executing the block not candidate / Process already ran 正式三事（477 余量）≠ bundled（不变量 595）；as a result of executing not Process already ran / not apply candidate / 477 finasresult item 2；FinalizeBlock must provide values provided values not empty keep current / not CheckTx 正式三事（477 余量）≠ bundled（不变量 596）；provided values not empty keep current / not CheckTx / 477 finasresult item 3；FinalizeBlock empty consensus_param_updates keep current not H+1 effective 正式三事（458 余量）≠ bundled（不变量 599）；empty consensus_param_updates keep current not H+1 / 458 finempty item 3；FinalizeBlock empty keep current not changed set / H+1 effective 正式三事（458 余量）≠ bundled（不变量 598）；empty keep current not changed set / H+1 / 458 finempty item 2；FinalizeBlock empty keep current not no must provide obligation 正式三事（458 余量）≠ bundled（不变量 597）；empty keep current not no must provide / 458 finempty item 1；Finalize 回包义务 not four gates settled 正式三事（363 余量）≠ bundled（不变量 600）；equiv not four gates settled / not finresp bundled / not no Prepare/Process / 363 finresp item 1；Finalize 回包义务 must provide 四列 not already changed set / settled 正式三事（363 余量）≠ bundled（不变量 601）；must provide 四列 not changed set / not settled / not finresp bundled / 363 finresp item 3；FinalizeBlock equiv ABCI 1.0 not Contains newly decided / apply candidate 正式三事（586 余量）≠ bundled（不变量 604）；equiv not Contains newly decided / not apply candidate / not finequiv bundled / 586 finequiv item 3；FinalizeBlock equiv ABCI 1.0 not no Prepare/Process 正式三事（586 余量）≠ bundled（不变量 603）；equiv not no Prepare/Process / not CheckTx skip Prepare/Process / not finequiv bundled / 586 finequiv item 2；FinalizeBlock equiv ABCI 1.0 not Contains newly decided / apply candidate 正式三事（586 余量）≠ bundled（不变量 604）；equiv not Contains newly decided / not apply candidate / not finequiv bundled / 586 finequiv item 3；FinalizeBlock equiv ABCI 1.0 not no Prepare/Process 正式三事（586 余量）≠ bundled（不变量 603）；equiv not no Prepare/Process / not CheckTx skip Prepare/Process / not finequiv bundled / 586 finequiv item 2；FinalizeBlock equiv ABCI 1.0 not four gates settled 正式三事（586 余量）≠ bundled（不变量 602）；equiv not four gates settled / not settled / not finequiv bundled / 586 finequiv item 1；FinalizeBlock When locks mempool not already settled / not four gates settled / not finlock bundled 正式三事（588 余量）≠ bundled（不变量 629）；locks mempool not already settled / not four gates settled / not finlock bundled 正式三事（588 余量）；FinalizeBlockResponse tx_results returned not Finalize changed already settled / not Code Data printed in this header LastResultsHash / not fintxcode bundled 正式三事（585 余量）≠ bundled（不变量 628）；tx_results returned not already settled / not Code Data printed in this header / not fintxcode bundled / 585 fintxcode item 3；FinalizeBlockResponse tx_results Code==0 only if fully valid not Code != 0 still in block / not no index / not fintxcode bundled 正式三事（585 余量）≠ bundled（不变量 627）；only if fully valid not Code != 0 still in block / not no index / not fintxcode bundled / 585 fintxcode item 2；FinalizeBlockResponse tx_results Code==0 only if fully valid not CheckTx passed / not Process Accept / not fintxcode bundled 正式三事（585 余量）≠ bundled（不变量 626）；only if fully valid not CheckTx passed / not Process Accept / not fintxcode bundled / 585 fintxcode item 1；FinalizeBlockResponse Query proofs anchored in this Merkle root not matched AppHash / not ProofOp key lookup / not finmerkle bundled 正式三事（475 余量）≠ bundled（不变量 625）；Query proofs anchored not matched AppHash / not ProofOp key lookup / not finmerkle bundled / 475 finmerkle item 3；FinalizeBlockResponse app_hash included as Header.AppHash in the next block not already written / not this header AppHash / not finmerkle bundled 正式三事（475 余量）≠ bundled（不变量 624）；included in next block not already written / not this header AppHash / not finmerkle bundled / 475 finmerkle item 2；FinalizeBlockResponse app_hash optional Merkle root not this header AppHash / not settled / not finmerkle bundled 正式三事（475 余量）≠ bundled（不变量 623）；MUST be deterministic not next_block_delay nondet / not 印进本头 / not finmerkle bundled / 475 finmerkle item 1；FinalizeBlockResponse MUST be deterministic not next_block_delay nondet / not 印进本头 / not finharddet bundled 正式三事（476 余量）≠ bundled（不变量 622）；MUST be deterministic not next_block_delay nondet / not 印进本头 / not finharddet bundled / 476 finharddet item 3；FinalizeBlockResponse app_hash may be hard-coded not Merkle root / not settled / not finharddet bundled 正式三事（476 余量）≠ bundled（不变量 621）；may be hard-coded not Merkle root / not settled / not finharddet bundled / 476 finharddet item 2；FinalizeBlockResponse app_hash may be empty not no state root / not settled / not finharddet bundled 正式三事（476 余量）≠ bundled（不变量 620）；may be empty not no state root / not settled / not finharddet bundled / 476 finharddet item 1； | 必学 no calls to CheckTx on new transactions not CheckTx optional / not already in pool / not finlock bundled 正式三事（588 余量）≠ bundled（不变量 630）；no calls to CheckTx on new transactions not CheckTx optional / not already in pool / not finlock bundled 正式三事（588 余量）； locks mempool after persist not Commit lock / not unlock / not Recheck / not finlock bundled 正式三事（588 余量）≠ bundled（不变量 631）；locks mempool after persist not Commit lock / not unlock / not Recheck / not finlock bundled 正式三事（588 余量）； FinalizeBlock When after unlock not unlock mempool / not finafter bundled / not When trigger 正式三事（593 余量）≠ bundled（不变量 643）； FinalizeBlock When CometBFT calls Commit not already settled / not four gates settled / not fincommit bundled 正式三事（590 余量）≠ bundled（不变量 644）； FinalizeBlock When instruct Application to persist its state not engine persist tx outputs / AppHash / ResultsHash / not Commit Usage signal bundled / not fincommit bundled 正式三事（590 余量）≠ bundled（不变量 645）； FinalizeBlock When calls Commit after lock mempool not Commit lock / not optional recheck / unlock / not fincommit bundled 正式三事（590 余量）≠ bundled（不变量 646）； OfferSnapshot Usage bootstrap accept/reject not OfferSnapshot bundled / not ListSnapshots / not Snapshot Connection 正式三事（499 余量）≠ bundled（不变量 647）； OfferSnapshot Usage upon accepting retrieve and apply not Offer 装完 / not Offer 收下之后 bundled / not LoadSnapshotChunk 正式三事（499 余量）≠ bundled（不变量 648）； OfferSnapshot Usage reject in chunk response not ABORT / not REJECT_SNAPSHOT / not Offer 收下之后 bundled 正式三事（499 余量）≠ bundled（不变量 649）；Only AppHash can be trusted not Snapshot metadata / not hash comparison / not OfferSnapshot app_hash 正式三事（483 余量）≠ bundled（不变量 650）；Any other data can be spoofed not hash comparison enough / not Snapshot Verification bundled / not ApplySnapshotChunk reject refetch DoS 正式三事（483 余量）≠ bundled（不变量 651）；verified AppHash at end not Info during load / not Transition to Consensus bundled / not Offer restored bundled 正式三事（483 余量）≠ bundled（不变量 652）； ApplySnapshotChunk Usage verify each chunk not Only AppHash can be trusted / not Snapshot Verification bundled / not ApplySnapshotChunk Result ACCEPT already complete 正式三事（485 余量）≠ bundled（不变量 653）； Info Usage app_version included in Header not last_block persisted during Commit / not Info response version / not AppHash in header 正式三事（494 余量）：[`../../tracks/implementation/worked-example-infousage-notappversion-vs-bundled.md`](../../tracks/implementation/worked-example-infousage-notappversion-vs-bundled.md)（不变量 670）。 Commit Usage Commit Usage Commit 里等 broadcast_tx not already can proceed / not already settled / not already allowed sync mempool in Commit 正式三事（310 余量）：[`../../tracks/implementation/worked-example-commitlock-notbroadcast-vs-bundled.md`](../../tracks/implementation/worked-example-commitlock-notbroadcast-vs-bundled.md)（不变量 691）。 Prepare 没有头哈希 not already have header hash / not Prepare already had hash / not already decided block identity 正式三事（311 余量）：[`../../tracks/implementation/候选不是已经是 ExecuteTxState not already ExecuteTxState / not already can name this height final / not already settled 正式三事（311 余量）：[`../../tracks/implementation/丢掉候选 not already can unboundedly accumulate / not already never need re-execute / not already bound by spec 正式三事（311 余量）：[`../../tracks/implementation/CheckTxState 不是已经按 ExecuteTxState 验过 not already checked against ExecuteTxState / not already checked against to-be-executed state / not already same as ExecuteTxState after reset 正式三事（312 余量）：[`../../tracks/implementation/同时在改不是已经同一份 not already same state / not already merged / not already shared working state 正式三事（312 余量）：[`../../tracks/implementation/RECHECK 不是已经是新交易 not already new transaction / not already treated as NEW / not already unlocked 正式三事（312 余量）：[`../../tracks/implementation/worked-example-checktxstate-notrecheck-vs-bundled.md`](../../tracks/implementation/worked-example-checktxstate-notrecheck-vs-bundled.md)（不变量 697；索引器去重不是已经保证不重复（不变量 698；过了 CheckTx 不是已经有应用级保护（不变量 699；通常不受欢迎不是已经没有幂等例外（不变量 700；Query 连接不是已经是 ExecuteTxState（不变量 701；上次 Commit 不是已经跟上正在跑的块（不变量 702；启动对齐不是已经是快照重放（不变量 703；MaxGas 不是已经在执行（不变量 704；GasUsed 不是已经算进共识（不变量 705；已提交块不是已经按气验过（不变量 706；结果列表不是已经同一顺序（不变量 707；Code 非零不是已经没进块（不变量 708；Code Data 不是已经印进本头（不变量 709；CheckTx Data 不是已经被引擎用了（不变量 710；各节点 Data 不一样不是已经分叉（不变量 711；Priority 不是已经是共识顺序（不变量 712；InitChain 空名单不是已经没有集合（不变量 713；同一批重复公钥不是已经能恢复（不变量 714；power 0 不是已经删掉不在集合里的人（不变量 715；InitChain 空参数不是已经没有参数（不变量 716；Finalize 没回不是已经清掉（不变量 717；只改一个字段不是已经只改这一项（不变量 718）））））））））））））））））））））。 worked-example-checktxstate-notsame-vs-bundled.md`](../../tracks/implementation/worked-example-checktxstate-notsame-vs-bundled.md)（不变量 696）。 worked-example-checktxstate-notexecute-vs-bundled.md`](../../tracks/implementation/worked-example-checktxstate-notexecute-vs-bundled.md)（不变量 695）。 worked-example-candidate-notdiscarded-vs-bundled.md`](../../tracks/implementation/worked-example-candidate-notdiscarded-vs-bundled.md)（不变量 694）。 worked-example-candidate-notexecute-vs-bundled.md`](../../tracks/implementation/worked-example-candidate-notexecute-vs-bundled.md)（不变量 693）。 worked-example-candidate-notheader-vs-bundled.md`](../../tracks/implementation/worked-example-candidate-notheader-vs-bundled.md)（不变量 692）。 lock mempool before Commit not already unlocked / not already sync done / not Commit return already released 正式三事（310 余量）：[`../../tracks/implementation/worked-example-commitlock-notunlocked-vs-bundled.md`](../../tracks/implementation/worked-example-commitlock-notunlocked-vs-bundled.md)（不变量 690）。 default global lock not already RPC safe / not already no concurrency / not already changed lock 正式三事（310 余量）：[`../../tracks/implementation/worked-example-commitlock-notrpcsafe-vs-bundled.md`](../../tracks/implementation/worked-example-commitlock-notrpcsafe-vs-bundled.md)（不变量 689）。 startup Info not already mid-height resume / not already can skip replay / not InitChain already done 正式三事（320 余量）：[`../../tracks/implementation/worked-example-crashsteps-notinfoskip-vs-bundled.md`](../../tracks/implementation/worked-example-crashsteps-notinfoskip-vs-bundled.md)（不变量 688）。 blockstore not already settled / not already Commit / not three steps already atomic 正式三事（320 余量）：[`../../tracks/implementation/worked-example-crashsteps-notblockstore-vs-bundled.md`](../../tracks/implementation/worked-example-crashsteps-notblockstore-vs-bundled.md)（不变量 687）。 app taller than engine not already allowed / not already can wake separately / not half-write atomic 正式三事（320 余量）：[`../../tracks/implementation/worked-example-crashsteps-notapptaller-vs-bundled.md`](../../tracks/implementation/worked-example-crashsteps-notapptaller-vs-bundled.md)（不变量 686）。 remember last Commit height not already app taller than engine / not already can skip replay / not Info handshake aligned 正式三事（335 余量）：[`../../tracks/implementation/worked-example-finpersist-notrememberheight-vs-bundled.md`](../../tracks/implementation/worked-example-finpersist-notrememberheight-vs-bundled.md)（不变量 685）。 MUST persist in Commit not already persisted in Finalize / not already unlocked / not Commit green can wait for broadcast 正式三事（335 余量）：[`../../tracks/implementation/worked-example-finpersist-notmustincommit-vs-bundled.md`](../../tracks/implementation/worked-example-finpersist-notmustincommit-vs-bundled.md)（不变量 684）。 FinalizeBlock changed state MUST NOT persist not already persisted / not already settled / not crash recovery block in store already Commit 正式三事（335 余量）：[`../../tracks/implementation/worked-example-finpersist-notmustnot-vs-bundled.md`](../../tracks/implementation/worked-example-finpersist-notmustnot-vs-bundled.md)（不变量 683）。 Historical blocks required for auditing replay light client not retain_height defaults to 0 retain all / not blocks below height may be removed / not full history in consensus when joined 正式三事（481 余量）：[`../../tracks/implementation/worked-example-commitpersist-nothistoricalblocks-vs-bundled.md`](../../tracks/implementation/worked-example-commitpersist-nothistoricalblocks-vs-bundled.md)（不变量 682）。 Expected persist at end of this call not Signal persist application state / not Commit empty request bundled / not signal already settled 正式三事（481 余量）：[`../../tracks/implementation/worked-example-commitpersist-notendofcall-vs-bundled.md`](../../tracks/implementation/worked-example-commitpersist-notendofcall-vs-bundled.md)（不变量 681）。 Signal persist application state not Finalize already persisted / not engine persist tx outputs AppHash ResultsHash / not expected persist at end of this call 正式三事（481 余量）：[`../../tracks/implementation/worked-example-commitpersist-notfinpersist-vs-bundled.md`](../../tracks/implementation/worked-example-commitpersist-notfinpersist-vs-bundled.md)（不变量 680）。 Historical blocks required for auditing replay light client not Use retain_height with caution / not If all nodes remove historical blocks / not Commit Usage persist signal bundled 正式三事（491 余量）：[`../../tracks/implementation/worked-example-commitretaincaution-notpersist-vs-bundled.md`](../../tracks/implementation/worked-example-commitretaincaution-notpersist-vs-bundled.md)（不变量 679）。 If all nodes remove historical blocks not retain_height defaults to 0 retain all / not bootstrap from genesis unless state sync / not Historical blocks required for auditing replay light client 正式三事（491 余量）：[`../../tracks/implementation/worked-example-commitretaincaution-notbootstrap-vs-bundled.md`](../../tracks/implementation/worked-example-commitretaincaution-notbootstrap-vs-bundled.md)（不变量 678）。 Commit Usage Use retain_height with caution not retain_height defaults to 0 retain all / not blocks below height may be removed / not Commit Usage persist signal bundled 正式三事（491 余量）：[`../../tracks/implementation/worked-example-commitretaincaution-notdefaultzero-vs-bundled.md`](../../tracks/implementation/worked-example-commitretaincaution-notdefaultzero-vs-bundled.md)（不变量 677）。 Echo Response Message the input string not Echo Usage test already done / not Request Message string to echo back / not Flush sync response 正式三事（492 余量）：[`../../tracks/implementation/worked-example-echousage-notdone-vs-bundled.md`](../../tracks/implementation/worked-example-echousage-notdone-vs-bundled.md)（不变量 676）。 Echo Request Message string to echo back not Response Message / not Echo request Message is Flush / not filled Message means echoed 正式三事（492 余量）：[`../../tracks/implementation/worked-example-echousage-notreqresp-vs-bundled.md`](../../tracks/implementation/worked-example-echousage-notreqresp-vs-bundled.md)（不变量 675）。 Echo Usage Echo a string to test implementation not Flush flush queue / not commit-empty-echo bundled / not Flush sync response 正式三事（492 余量）：[`../../tracks/implementation/worked-example-echousage-notflush-vs-bundled.md`](../../tracks/implementation/worked-example-echousage-notflush-vs-bundled.md)（不变量 674）。 Info Usage Used to sync during handshake not Info 握手 bundled / not QueryState snapshot replay / not Info request version handshake verified 正式三事（494 余量）：[`../../tracks/implementation/worked-example-infousage-nothandshake-vs-bundled.md`](../../tracks/implementation/worked-example-infousage-nothandshake-vs-bundled.md)（不变量 669）。 Info Usage Return information about application state not QueryState / not Info response data arbitrary info / not handshake sync already persisted 正式三事（494 余量）：[`../../tracks/implementation/worked-example-infousage-notquerystate-vs-bundled.md`](../../tracks/implementation/worked-example-infousage-notquerystate-vs-bundled.md)（不变量 668）。 Info Usage lane_priorities empty iff default_lane empty not Info 车道 bundled / not default_lane in table / not CheckTx lane_id in range 正式三事（497 余量）：[`../../tracks/implementation/worked-example-infousage-notemptyiff-vs-bundled.md`](../../tracks/implementation/worked-example-infousage-notemptyiff-vs-bundled.md)（不变量 667）。 Info Usage does not have to define lane_priorities not Info 车道 bundled / not CheckTx empty lane_id default lane / not CheckTx Priority consensus order 正式三事（497 余量）：[`../../tracks/implementation/worked-example-infousage-notlaneoptional-vs-bundled.md`](../../tracks/implementation/worked-example-infousage-notlaneoptional-vs-bundled.md)（不变量 666）。 Info Usage last_block persisted during Commit not Info 握手 bundled / not Commit persist signal / not Info response last_block fields 正式三事（497 余量）：[`../../tracks/implementation/worked-example-infousage-notcommitpersist-vs-bundled.md`](../../tracks/implementation/worked-example-infousage-notcommitpersist-vs-bundled.md)（不变量 665）。 Info Usage priority 0 reserved for empty lane_id not Info 车道 bundled / not CheckTx empty lane_id default lane / not CheckTx Priority consensus order 正式三事（498 余量）：[`../../tracks/implementation/worked-example-infousage-notpriorityzero-vs-bundled.md`](../../tracks/implementation/worked-example-infousage-notpriorityzero-vs-bundled.md)（不变量 664）。 Info Usage default_lane has to be one of identifiers defined in lane_priorities not Info 车道 bundled / not empty iff / not CheckTx lane_id in range 正式三事（498 余量）：[`../../tracks/implementation/worked-example-infousage-notintable-vs-bundled.md`](../../tracks/implementation/worked-example-infousage-notintable-vs-bundled.md)（不变量 663）。 ListSnapshots Usage See Snapshot data type for details not Snapshot 类型 bundled / not Offer 装完 / not ListSnapshots 本地清单就已经是同一份 正式三事（500 余量）：[`../../tracks/implementation/worked-example-listsnapusage-notsnaptype-vs-bundled.md`](../../tracks/implementation/worked-example-listsnapusage-notsnaptype-vs-bundled.md)（不变量 662）。 ListSnapshots Usage discover on peers not ListSnapshots 空请求 bundled / not ListSnapshots 本地清单 bundled / not Snapshot Discovery 正式三事（500 余量）：[`../../tracks/implementation/worked-example-listsnapusage-notdiscover-vs-bundled.md`](../../tracks/implementation/worked-example-listsnapusage-notdiscover-vs-bundled.md)（不变量 661）。 LoadSnapshotChunk Usage retrieve snapshot chunks not ApplySnapshotChunk chunk 栏 / not ACCEPT bundled / not LoadSnapshotChunk 已经齐 正式三事（501 余量）：[`../../tracks/implementation/worked-example-loadsnapusage-notchunks-vs-bundled.md`](../../tracks/implementation/worked-example-loadsnapusage-notchunks-vs-bundled.md)（不变量 660）。 LoadSnapshotChunk Usage retrieve from peers not ListSnapshots discover / not ListSnapshots 空请求 bundled / not Snapshot Discovery 正式三事（501 余量）：[`../../tracks/implementation/worked-example-loadsnapusage-notdiscover-vs-bundled.md`](../../tracks/implementation/worked-example-loadsnapusage-notdiscover-vs-bundled.md)（不变量 659）。 LoadSnapshotChunk Usage Used during state sync to retrieve not LoadSnapshotChunk bundled / not Offer 装完 / not Transition to Consensus 正式三事（501 余量）：[`../../tracks/implementation/worked-example-loadsnapusage-notretrieve-vs-bundled.md`](../../tracks/implementation/worked-example-loadsnapusage-notretrieve-vs-bundled.md)（不变量 658）。 ApplySnapshotChunk Usage will not do unless instructed not refetch_chunks bundled / not reject_senders bundled / not unable retrieve OfferSnapshot 正式三事（502 余量）：[`../../tracks/implementation/worked-example-applysnapusage-notunless-vs-bundled.md`](../../tracks/implementation/worked-example-applysnapusage-notunless-vs-bundled.md)（不变量 657）。 ApplySnapshotChunk Usage can choose refetch/ban not refetch_chunks bundled / not reject_senders bundled / not RETRY bundled 正式三事（502 余量）：[`../../tracks/implementation/worked-example-applysnapusage-notchoose-vs-bundled.md`](../../tracks/implementation/worked-example-applysnapusage-notchoose-vs-bundled.md)（不变量 656）。 ApplySnapshotChunk Usage unable to retrieve next chunk not refetch/reject_senders / not REJECT_SNAPSHOT bundled / not already matched LastBlockAppHash 正式三事（485 余量）：[`../../tracks/implementation/worked-example-applysnapusage-notunable-vs-bundled.md`](../../tracks/implementation/worked-example-applysnapusage-notunable-vs-bundled.md)（不变量 655）。 ApplySnapshotChunk Usage all chunks accepted 后 Info not Info during load / not Transition to Consensus bundled / not verified AppHash at end 正式三事（485 余量）≠ bundled（不变量 654）； FinalizeBlock When round 0 not same round / not next_block_delay / not timeout_commit / not finh1 bundled 正式三事（593 余量）≠ bundled（不变量 642）； FinalizeBlock When starts consensus for height h+1 not already settled / not persist decision / not finh1 bundled 正式三事（593 余量）≠ bundled（不变量 641）； FinalizeBlock When unlock after optional recheck not Commit lock / not h+1 round 0 / not finunlock bundled 正式三事（592 余量）≠ bundled（不变量 640）； FinalizeBlock When newly received transactions can now be checked not optional recheck outstanding txs / not CheckTx optional / not finunlock bundled 正式三事（592 余量）≠ bundled（不变量 639）； FinalizeBlock When unlocks the mempool not already settled / not four gates settled / not finunlock bundled 正式三事（592 余量）≠ bundled（不变量 638）； FinalizeBlock When against newly persisted Application state not Type=RECHECK / not CheckTxState / ExecuteTxState / not finrecheck bundled 正式三事（591 余量）≠ bundled（不变量 637）； FinalizeBlock When all outstanding transactions in the mempool not new transactions / not CheckTx passed forever valid / not finrecheck bundled 正式三事（591 余量）≠ bundled（不变量 636）； FinalizeBlock When optionally re-checks not must recheck / not already settled / not finrecheck bundled 正式三事（591 余量）≠ bundled（不变量 635）； Finalize 之后 optional recheck unlock h+1 not Recheck / not unlock / not finafter bundled 正式三事（403 余量）≠ bundled（不变量 634）；Finalize 之后落完再锁内存池、新交易不进 CheckTx not Commit lock / not already settled / not finafter bundled 正式三事（403 余量）≠ bundled（不变量 633）；Finalize 之后引擎才落盘 tx outputs / AppHash / ResultsHash not already settled / not Commit persist application state / not finafter bundled 正式三事（403 余量）≠ bundled（不变量 632）；Finalize 之后引擎才落盘 tx outputs / AppHash / ResultsHash not already settled / not Commit persist application state / not finafter bundled 正式三事（403 余量）； |

---

## 安全与失败资产

| 编号 | 资产 | 路径 | 状态 | 优先级 |
|---|---|---|---|---|
| 06 | 《区块链失败博物馆》 | `tracks/failure-museum/` | 九十二案 + 17144 五层精读 + 停链面地图 | 必学 |
| 07 | 《Design Pattern Library》 | `libraries/design-patterns/` | 329 条 | 重要 |
| 08 | 《Anti-Pattern Library》 | `libraries/anti-patterns/` | 436 条 | 重要 |

---

## 「不确定」专用资产

| 编号 | 资产 | 路径 | 状态 | 优先级 |
|---|---|---|---|---|
| 09 | 《后量子区块链工程手册》 | `tracks/post-quantum/` | FIPS 名义长度 + 外部 ctx + 有状态 HBS 卡；CPU 空 | 研究级 |
| 10 | 《不确定协议设计决策库》 | `libraries/decision-matrix/` | 对照列已扩；候选列空 | 重要 |
| 11 | 《不确定威胁模型》 | `libraries/threat-model/` | 草稿 | 必学 |
| 12 | 《不确定 Invariant Library》 | `libraries/invariants/` | 592 条 | 必学 |
| 13 | 《不确定 Adversarial Test Corpus》 | `libraries/adversarial-corpus/` | 目录 C01–C605；runner 未建 | 重要 |
| 14 | 《不确定长期技术路线图》 | `courses/level-10-uncertain-studio/` | 建议清单，非选型 | 重要 |

---

## 过程资产

| 资产 | 路径 | 作用 |
|---|---|---|
| 最高准则 | [`../GOAL.md`](../GOAL.md) | 门禁 |
| 通读顺序 | [`04-study-path.md`](04-study-path.md) | 目的 A 的六通，不是浏览 |
| MASTER ROADMAP | [`00-master-roadmap.md`](00-master-roadmap.md) | 怎么走完 Level 0–10 |
| 知识树 | [`01-knowledge-tree.md`](01-knowledge-tree.md) | 硬依赖 |
| 研究顺序 | [`02-research-order.md`](02-research-order.md) | 为什么不按名气 |
| 审核日志 | [`../AUDIT_LOG.md`](../AUDIT_LOG.md) | 对自己写的东西负责 |

---

## 每条核心链档案

| 链 / 品类 | 波次 | 优先级 | 档案状态 |
|---|---|---|---|
| Bitcoin | 1 | 必学 | 第一版；策略 ≠ 共识（不变量 144）；txid ≠ wtxid（不变量 152）；钥匙路径 ≠ 揭树（不变量 153）；进了块的 coinbase ≠ 已经能花（不变量 163）；脚本里的 CLTV ≠ 交易 nLockTime 已经把输出锁住（不变量 164）；脚本里的 CSV ≠ 绝对锁 / ≠ 部署名（不变量 165）；选择加入替换信号 ≠ 已经换掉（不变量 166）；付给脚本哈希 ≠ 已经揭开赎回脚本（不变量 170）；版本位被置上 ≠ 已经锁定 / 锁定 ≠ 已经激活（不变量 171）；ECDSA 验得过 ≠ 已经是严格 DER（不变量 172）；coinbase 高度 ≠ 头上已经有高度字段（不变量 173）；看见 Bech32 地址串 ≠ 链上已经有这笔输出（不变量 174）；看见部分签名包 ≠ 已经能广播（不变量 179）；后继校验过了 ≠ 已经是旧校验那套地址（不变量 181）；看见扩展公钥 ≠ 已经能花（不变量 182）；看见助记词 ≠ 已经是二进制种子（不变量 183）；看见私钥或助记词备份 ≠ 已经知道该看哪种输出脚本（不变量 184）；看见后继版本工作包 ≠ 已经是旧版那份固定未签交易（不变量 186）；走脚本路径 ≠ 已经是 tapscript 语义（不变量 189）；看见 Miniscript ≠ 已经是链上脚本（不变量 191）；看见机会主义未认证加密 ≠ 已经私人 / 伪随机字节流 ≠ 已经认不出 / 仍收下第 1 版 ≠ 已经退役旧线（不变量 242）；看见客户端侧过滤器对上 ≠ 已经有块 / 过滤器头链对上 ≠ 已经写进共识 / 至少一个诚实对等节点 ≠ 已经验完脚本（不变量 243）；看见基本过滤器对上 ≠ 已经在集合里 / 装了花费脚本和收款脚本 ≠ 已经有那笔交易 / 排除了 OP_RETURN ≠ 已经写进共识（不变量 244）；看见跳过库存通告 ≠ 已经拒进池 / 发了费率过滤器 ≠ 对等节点已经照做 / 布隆过了 ≠ 已经过了费率门（不变量 245）；看见后继地址流言 ≠ 已经连得上 / 发了 sendaddrv2 ≠ 已经只收后继格式 / 在传某种网上的地址 ≠ 已经连上那种网（不变量 246）；看见发了 sendheaders ≠ 已经改用头通告 / 用头通告新尖 ≠ 已经有块 / 重组时先发头 ≠ 中间块已经在手里（不变量 247）；看见按 wtxid 通告 ≠ 已经有那笔交易 / 发了 wtxidrelay ≠ 已经改口 / 仍用旧类型要父交易 ≠ 旧库存已经退役（不变量 248）；看见对账素描 ≠ 已经有那些交易 / 发了 sendtxrcncl ≠ 已经在对账 / 对账失败退回洪水 ≠ 库存通告已经退役（不变量 249）；看见有限服务位 ≠ 已经能服任意旧块 / 只保证最近窗口 ≠ 已经剪枝 / 服了最近一块 ≠ 已经暴露了剪到哪（不变量 250）；看见带见证的线上序列化 ≠ 已经有见证 / 能提供见证 ≠ 已经在传 / 库存通告仍用旧类型 ≠ 线上已经没有见证（不变量 251）；看见没开布隆服务位 ≠ 已经退役 / 开了这一位 ≠ 已经私人 / 协议版本够了 ≠ 已经在遵守（不变量 252）；看见内存池查询回了一串库存 ≠ 已经有那些交易 / 只肯给最近转发过的 ≠ 已经支持整池查询 / 协议版本够了 ≠ 已经在答（不变量 253）；看见拒收消息 ≠ 已经共识非法 / 调试理由 ≠ 已经该给用户看 / 没拒收 ≠ 已经是当前最好链（不变量 254）；看见付款 URI ≠ 已经授权 / 路径没有链上地址 ≠ 已经没有指示 / 不认识的必选参数 ≠ 已经能付（不变量 255）；看见版本里关掉交易转发 ≠ 已经终身只传块 / 发了停交易转发 ≠ 已经没有紧凑块 / 建议关掉地址 ≠ 已经禁止（不变量 256）；看见同一交易标识 ≠ 已经唯一 / 许多确认 ≠ 已经不怕被覆盖 / 已经花光后再出现 ≠ 已经非法（不变量 257）；看见签过的消息 ≠ 已经控制资金 / 资金证明清单 ≠ 已经齐（不变量 258）；看见协议版本够了 ≠ 已经支持某项功能 / 通告了 feature ≠ 已经启用（不变量 259）；看见静默付款地址 ≠ 已经有输出 / 扫过 ≠ 已经收到（不变量 260）；看见可读名字 ≠ 已经该走 DNS / TXT ≠ 已经是合法付款指示（不变量 261）；看见协议版本够了 ≠ 已经会带 nonce 的 ping / pong ≠ 已经还活着（不变量 262）；看见协议版本 ≠ 已经是客户端版本 / user agent ≠ 已经可以按实现改行为（不变量 263）；看见多余栈元素 ≠ 已经随便填 / 隔离见证 ≠ 已经没有这条延展（不变量 264）；看见 signet ≠ 已经是 testnet / 已经是 regtest / 头上工作量 ≠ 已经签过（不变量 265）；看见 BIP32 compatible ≠ 已经能互操作 / 自称 BIPxx compatible ≠ 已经是那份结构（不变量 266）；看见同一份种子 ≠ 已经是同一条币 / 余额为零 ≠ 已经发现完（不变量 267）；看见同一套 BIP44 账户 ≠ 已经能找回嵌套隔离见证 / 账户出现了 ≠ 已经不用核余额（不变量 268）；看见脚本类型层 ≠ 已经是账户层 / 现有多签习惯 ≠ 已经要搬家（不变量 269）；看见同一套钥 ≠ 已经是同一条 P2SH 地址 / 未压缩钥 ≠ 已经是本页（不变量 270）；看见共享主公钥 ≠ 已经是本页 / 前面分支没有交易 ≠ 已经发现完（不变量 271）；看见派生钥 ≠ 已经是输出钥 / 不需要脚本路径 ≠ 已经不承诺（不变量 272）；看见付款码 ≠ 已经是存款地址 / 通知输出 ≠ 已经能花（不变量 273）；看见 multi ≠ 已经按字典序排 / 门限 ≠ 已经同一套上限（不变量 274）；看见 tr 没有树 ≠ 已经有脚本路径 / 压缩钥 ≠ 已经是 x-only（不变量 275）；看见 pk ≠ 已经和 pkh / sh 同一套放置 / 熟悉脚本 ≠ 已经能互操作（不变量 276）；看见 wpkh / wsh ≠ 已经只能顶层 / 未压缩钥 ≠ 已经允许（不变量 277）；看见 multi_a ≠ 已经是 383 那种 multi / 门限 ≠ 已经同一套编码（不变量 278）；看见旧 PSBT 栏 ≠ 已经能装 Taproot / 输出脚本里的钥 ≠ 已经是内部钥（不变量 279）；看见钱包策略 ≠ 已经是一条描述符 / 登记过 ≠ 已经批准这笔花（不变量 280）；看见 combo ≠ 已经只能产出一种脚本 / 未压缩钥 ≠ 已经带齐见证对（不变量 281）；看见 raw ≠ 已经能套进具名表达式 / addr ≠ 已经是那份输出脚本（不变量 282）；看见聚合钥 ≠ 已经是扩展公钥 / 合成扩展公钥 ≠ 已经能硬化派生（不变量 283）；看见旧 PSBT 栏 ≠ 已经能装 MuSig2 / 聚合钥栏 ≠ 已经是输出钥（不变量 284）；看见脚本各走各的路径 ≠ 已经是多签该有的树 / 路径里的脚本类型 ≠ 已经必要（不变量 285）；看见一份助记词 ≠ 已经能备齐所有钱包 / 派生出的熵 ≠ 已经是目标钱包的种子（不变量 286）；看见部分签名包 ≠ 已经是跨厂安全多签开户 / TOKEN ≠ 已经是钱包种子（不变量 287）；看见一条派生路径 ≠ 已经是一份路径模板 / 写死熟路径 ≠ 已经能互操作（不变量 288）；看见共享了扩展公钥 ≠ 已经是链码委托 / 本笔微调 ≠ 已经是盲签（不变量 289）；带 pj= 的付款 URI ≠ 已经是 payjoin 付款 / 原始包 ≠ 已经是提案 / 收款方加了输入 ≠ 已经另开一笔（不变量 290）；自家习惯的输入输出顺序 ≠ 已经是字典序标准 / 按字典序排了 ≠ 已经是共识 / ≠ 已经私人（不变量 291）；Testnet 4 ≠ 已经是 Testnet 3 / 20 分钟例外 ≠ 已经没有块风暴 / 会 Testnet 3 ≠ 已经能安全跟（不变量 292）；储备证明交易 ≠ 已经能花 / 其余输入签过 ≠ 已经控制资金 / POR 栏 ≠ 已经是普通花费（不变量 293）；本页这种签消息 ≠ 已经是 322 / 头字节标了种类 ≠ 已经有地址 / 旧 P2PKH 习惯 ≠ 已经互操作（不变量 294）；付款请求 ≠ 已经授权 / 付款报文 ≠ 已经是回执 / 回执 ≠ 已经最终（不变量 295）；加密私钥记录 ≠ 已经能用 / 厂家代生成 ≠ 已经能兑 / 地址哈希片段 ≠ 已经是地址（不变量 296）；本页这种地址 ≠ 已经是赎回脚本 / 旧软件报无效 ≠ 已经付过 / 只有地址 ≠ 已经知道付给谁（不变量 297） |
| Cosmos / CometBFT | 2 | 必学 | 第一版；FinalizeBlock equiv ABCI 1.0 not Contains newly decided / apply candidate 正式三事（586 余量）≠ bundled（不变量 604）；FinalizeBlock equiv ABCI 1.0 not no Prepare/Process 正式三事（586 余量）≠ bundled（不变量 603）；FinalizeBlock equiv ABCI 1.0 not four gates settled 正式三事（586 余量）≠ bundled（不变量 602）；Finalize 回包义务 must provide 四列 not already changed set / settled 正式三事（363 余量）≠ bundled（不变量 601）；Finalize 回包义务 not four gates settled 正式三事（363 余量）≠ bundled（不变量 600）；IBC 客户端 ≠ 数据包（不变量 146）；本头 AppHash ≠ 本块已交差（不变量 147）；本头 LastCommit ≠ 本高已 +2/3（不变量 148）；源链托管 ≠ 对岸已经铸出原币（不变量 155）；写下每条消息 ≠ 已经 fsync / 回放时又要签 ≠ 已经双签（不变量 298）；先装证据 ≠ 已经装满交易 / 两条收交易上限 ≠ 已经同一条（不变量 299）；本地 State ≠ 已经进了块 / 头上的根 ≠ 已经有了 State（不变量 300）；提案收了 ≠ 已经从池里删掉 / CheckTx 过了 ≠ 已经永远有效（不变量 301）；同一高度换轮 ≠ 已经换了集合 / 新加入 ≠ 已经能跳到队头（不变量 302）；创世 app_state ≠ 已经验过应用状态 / 进程起来 ≠ 已经过了 genesis_time（不变量 303）；票上 Timestamp ≠ 已经验过 / 冲突提案 ≠ 已经有证据（不变量 304）；InitPeer ≠ 已经能交互 / Receive ≠ 已经 AddPeer（不变量 305）；Peer 句柄 ≠ 已经是那个人 / Broadcast 回了通道 ≠ 已经送到（不变量 306）；同进程 ≠ 已经隔离 / 一条连接 ≠ 已经是四门（不变量 307）；NumPeers ≠ 已经数完 / 按名字拿到反应堆 ≠ 已经独立（不变量 308）；HasChannel ≠ 已经入队 / Send 回假 ≠ 已经断开（不变量 309）；默认锁 ≠ 已经 RPC 安全 / Commit 里等广播 ≠ 已经能往下走（不变量 310）；候选 ≠ 已经是 ExecuteTxState / Prepare 没有头哈希 ≠ 已经知道本头（不变量 311）；CheckTxState ≠ 已经是 ExecuteTxState / RECHECK ≠ 已经是新交易（不变量 312）；内存池去重 ≠ 已经保证不重放 / 过了 CheckTx ≠ 已经有应用级保护（不变量 313）；QueryState ≠ 已经是 ExecuteTxState / 启动对齐 ≠ 已经是快照重放（不变量 314）；MaxGas ≠ 已经在执行 / GasUsed ≠ 已经算进共识（不变量 315）；结果列表 ≠ 已经同一顺序 / Code 非零 ≠ 已经没进块（不变量 316）；CheckTx 的 Data ≠ 已经被引擎用了 / Priority ≠ 已经是共识顺序（不变量 317）；InitChain 空名单 ≠ 已经没有集合 / 同一批重复公钥 ≠ 已经能恢复 / power 0 ≠ 已经删掉不在集合里的人（不变量 318）；InitChain 空参数 ≠ 已经没有参数 / Finalize 没回 ≠ 已经清掉 / 只改一个字段 ≠ 已经只改这一项（不变量 319）；应用高度比引擎高 ≠ 已经允许 / 块进 store ≠ 已经 Commit / 启动 Info 对上 ≠ 已经能跳步（不变量 320）；Offer 收下 ≠ 已经装完 / 一块 chunk 收下 ≠ 已经齐 / 拉失败换一份 ≠ 已经能接着装（不变量 321）；ListSnapshots 回了 ≠ 已经有了全部快照 / 挑了最高 ≠ 已经收下 / Offer 被拒 ≠ 已经停（不变量 322）；装完 ≠ 已经有了 ChainID / AppHash 对上 ≠ 已经版本也对上 / 切进共识 ≠ 已经有完整历史（不变量 323）；拍了这个高度 ≠ 已经交差之后拍的 / 没停链 ≠ 已经一致 / 只留最近两份 ≠ 已经有了全部历史快照（不变量 324）；头上有 AppHash ≠ 已经是交易默克尔 / Query 回了 Proof ≠ 已经对上 AppHash / 一层 ProofOp 的根 ≠ 已经对上最终 AppHash（不变量 325）；发了 addr 过滤查询 ≠ 已经收下这个人 / id 过滤查询绿了 ≠ 已经过了 addr / 有 /store 路径 ≠ 已经是引擎在用（不变量 326）；立刻整块执行 ≠ 已经离开提议超时的关键路径（不变量 327）；同一高度回了不同码 ≠ 已经有了 CheckTxCode（不变量 328）；Query 回了 ≠ 已经复制到各节点（不变量 329）；到了 H ≠ 已经 Prepare 带了扩展（不变量 330）；填了证据 MaxBytes ≠ 已经落在块上限下面（不变量 331）；装完又对上 LastBlockAppHash ≠ 已经在装回当中验过（不变量 332）；本高回了 ConsensusParams ≠ 已经在本高生效（不变量 333）；四门里有 Snapshot Connection ≠ 已经必须实现快照（不变量 334）；Finalize 改了状态 ≠ 已经落盘（不变量 335）；填了 Precision ≠ 已经是 MessageDelay（不变量 336）；-1 就按 100 MB 验 ≠ 已经没有上限（不变量 337）；Prepare 没有确定性要求 ≠ 已经必须确定（不变量 338）；不该验排序相关有效性 ≠ 已经该在 CheckTx 里验（不变量 339）；Process 必须只依赖请求和上一份状态 ≠ 已经可以像 Prepare 那样依赖其它值（不变量 340）；Verify 必须只依赖扩展、这块和上一份状态 ≠ 已经可以像 ExtendVote 那样依赖其它值（不变量 341）；Finalize 算出的状态必须只依赖上一份状态和决定块 ≠ 已经可以像 Prepare 那样依赖其它值（不变量 342）；写成 0 不是已经启用 PBTS ≠ 已经填了 Precision 就是 PBTS（不变量 343）；MaxBytes 减去头集合证据才是交易上限 ≠ 已经整块都能装交易（不变量 344）；整池可见 ≠ 已经只能看见装得进一块的子集（不变量 345）；必须协调升级 ≠ 已经只改 VoteExtensionsEnableHeight（不变量 346）；正确提议者的准备提案必须被正确接收者 Accept ≠ 已经是任意块都会 Accept（不变量 347）；正确进程交出的扩展必须被正确接收者 Verify Accept ≠ 已经是任意扩展都会 Accept（不变量 348）；Prepare 不得改已提交状态 ≠ 已经立刻执行就已经交差（不变量 349）；一轮最多一张 Precommit ≠ 已经能再签一张（不变量 350）；Process 也会在提议者那边叫 ≠ 已经不用再 Process（不变量 351）；+2/3 之后才进来的扩展写进了 commit info ≠ 已经 Verify 过（不变量 352）；空扩展仍会调 Verify ≠ 已经跳过 Verify（不变量 353）；Process 调用是同步的 ≠ 已经能在返回之后再改裁决（不变量 354）；从提案拿掉 tx ≠ 已经从内存池删掉（不变量 355）；validValue 非 nil ≠ 已经还会调 Prepare（不变量 356）；引擎没有再验重复交易 ≠ 已经验过重复（不变量 357）；vote_extension 会包进 CanonicalVoteExtension ≠ 已经按原样签（不变量 358）；Prepare 和 Process / Finalize 同一套字段 ≠ 已经跑过 Process（不变量 359）；至少一名非拜占庭验证者跑过 Process ≠ 已经每个验证者都跑过 Process（不变量 360）；+2/3 prevote 同一 id(v) 才锁住再调 ExtendVote ≠ 已经会调 ExtendVote（不变量 361）；+2/3 precommit 同一 id(v) 才决定再调 Finalize ≠ 已经会调 Finalize（不变量 362）；Finalize 等价于 ABCI 1.0 那三步 ≠ 已经是四门已经结算（不变量 363）；Validator 用 address 认人 ≠ 已经带了公钥（不变量 364）；VoteInfo 能按到场定奖惩 ≠ 已经罚没（不变量 365）；retain_height 默认 0 ≠ 已经在剪（不变量 366）；没定义 lane_priorities ≠ 已经排了优先（不变量 367）；快照全字段（含 Metadata）对上 ≠ 已经装完（不变量 368）；ExtendedVoteInfo 从本进程抽出 ≠ 已经从块里抽出（不变量 369）；Info 用来握手对齐 ≠ 已经是快照重放（不变量 370）；Query 可以对当前或过去高度查 ≠ 已经是 QueryState（不变量 371）；Misbehavior.type 只是过错枚举 ≠ 已经罚没（不变量 372）；CheckTx 技术上可选、不参与处理块 ≠ 已经是四门已经结算（不变量 373）；Flush 要把客户端排队的消息冲到服务端 ≠ 已经送到（不变量 374）；LoadSnapshotChunk 用来从邻居拉快照块 ≠ 已经齐（不变量 375）；UNKNOWN 一律是错、引擎当应用坏了会崩 ≠ 已经是四门已经结算（不变量 376）；data 按 URI 查询分量解释、可以和 path 一起或代替 path 用 ≠ 已经是 Query 高度（不变量 377）；应用可以再拉块或封邻居、引擎不自己做 ≠ 已经封了（不变量 378）；Info 请求 version 是 CometBFT 软件语义版本 ≠ 已经是 app_version（不变量 379）；Query 回包 index 是树里这个键的下标 ≠ 已经是按键查（不变量 380）；CheckTx 回包 codespace 是码的命名空间 ≠ 已经是回包码（不变量 381）；syncing_to_height 同步或重放时是目标高、否则等于本高 ≠ 已经有完整历史（不变量 382）；Query 请求 prove 是能回就回默克尔证明 ≠ 已经对上 AppHash（不变量 383）；Query 回包 code 是回包码 ≠ 已经过了共识（不变量 384）；ConsensusParams.block 限制块大小和块间隔 ≠ 已经是 MaxBytes 上限（不变量 385）；ConsensusParams.evidence 限制拜占庭证据是否合法 ≠ 已经是证据 MaxBytes（不变量 386）；InitChain 请求 time 是创世时间 ≠ 已经过了 genesis_time（不变量 387）；InitChain 请求 consensus_params 是起步共识参数 ≠ 已经没有参数（不变量 388）；Info 回包 data 是任意信息 ≠ 已经是握手对齐（不变量 389）；ProofOp.key 是这棵默克尔树里这把键 ≠ 已经是 Query 回包键（不变量 390）；CheckTx 请求 tx 是请求交易字节 ≠ 已经是 Recheck（不变量 391）；InitChain 回包 app_hash 是起步应用哈希 ≠ 已经是本头 AppHash（不变量 392）；ExecTxResult.gas_wanted 是这笔要的气 ≠ 已经是 CheckTx 的 GasWanted（不变量 393）；ExtendedCommitInfo.round 是提交轮 ≠ 已经是 CommitInfo.round（不变量 394）；ListSnapshots 请求是空请求、向应用要一份快照清单 ≠ 已经齐（不变量 395）；OfferSnapshot 请求 snapshot 是拿来装回的那份快照 ≠ 已经是本地清单（不变量 396）；ApplySnapshotChunk 请求 chunk 是 LoadSnapshotChunk 回的那块二进制内容 ≠ 已经在拉块（不变量 397）；ApplySnapshotChunk Result RETRY 是再装这块、按需配合 RefetchChunks 和 RejectSenders ≠ 已经再拉（不变量 398）；Commit 不带参数 ≠ 已经落盘（不变量 399）；OfferSnapshot Result REJECT_FORMAT 是拒掉这种 format、换一份 ≠ 已经是拒掉这份（不变量 400）；Offer 收下之后才去拉块并装 ≠ 已经装完（不变量 401）；OfferSnapshot Result UNKNOWN 是结果不明、中止全部装回 ≠ 已经崩（不变量 402）；Finalize 之后引擎才落盘各笔输出 / AppHash / ResultsHash ≠ 已经交差（不变量 403）；Finalize 回包 app_hash 可以空或硬编码、但必须确定 ≠ 已经印进本头（不变量 404）；CheckTx 是内存池的守卫、每条节点先跑 CheckTx 才让交易进本地池 ≠ 已经是技术上可选（不变量 405）；Snapshot.height 是拍快照的高度（Commit 之后） ≠ 已经是 Query 高度（不变量 406）；Finalize 含刚决定那块的字段 ≠ 已经是四门已经结算（不变量 407）；Finalize 按应用自己的规则确定地执行 txs、再交还控制权 ≠ 已经交差（不变量 408）；ExtendVoteRequest 的内容对应共识即将发 Precommit 的那份拟议块 ≠ 已经会调 ExtendVote（不变量 409）；ExtendVoteRequest.hash 是扩展要指的那份拟议块头哈希 ≠ 已经跑过 Process（不变量 410）；ExtendVoteRequest.txs 是扩展要指的那份块的交易列表 ≠ 已经执行那些交易（不变量 411）；InitChain 创世时只调一次 ≠ 已经是崩溃后再调（不变量 412）；ExtendVoteRequest.misbehavior 是拟议块里那些过错信息 ≠ 已经定奖惩（不变量 413）；ExecTxResult.log 是应用日志的输出 ≠ 已经是 Query 日志（不变量 414）；VerifyVoteExtensionRequest.height 是块高度（用来对一下） ≠ 已经是拟议块高度（不变量 415）；进了这一轮会先设 ProposeTimeout ≠ 已经填了 TimeoutPropose（不变量 416）；自己是提议者会先走完 Prepare 那五步 ≠ 已经不用再 Process（不变量 417）；ExtendVoteResponse.vote_extension 是 CometBFT 签的信息、可以 0 长、标成非确定 ≠ 已经会包进 CanonicalVoteExtension（不变量 418）；ProcessProposalRequest.txs 是拟议块的交易列表 ≠ 已经执行那些交易（不变量 419）；ProcessProposalRequest.proposed_last_commit 是从拟议块里的信息拿到的上一份提交信息 ≠ 已经交差 local_last_commit（不变量 420）；ExtendedVoteInfo.vote_extension 是发送验证者的应用给的非确定扩展 ≠ 已经从本进程抽出（不变量 421）；FinalizeBlockRequest.decided_last_commit 是从刚决定那块拿到的上一份提交信息 ≠ 已经交差 local_last_commit（不变量 422）；PrepareProposalRequest.max_tx_bytes 是当前配置的、改过的交易占的最大字节 ≠ 已经能回超限列表（不变量 423）；PrepareProposalRequest.local_last_commit 是从本进程 CometBFT 数据结构拿到的上一份提交信息 ≠ 已经交差 proposed_last_commit（不变量 424）；ExtendedVoteInfo.validator 是发了这张票的验证者 ≠ 已经带了公钥（不变量 425）；PrepareProposalRequest.next_validators_hash 是下一验证者集合默克尔根 ≠ 已经是 Finalize 请求栏的 next_validators_hash（不变量 426）；ProcessProposalRequest.next_validators_hash 是下一验证者集合默克尔根 ≠ 已经是 Prepare 请求末栏的 next_validators_hash（不变量 427）；FinalizeBlockRequest.hash 是已决块的哈希 ≠ 已经是 ProcessProposalRequest.hash（不变量 428）；FinalizeBlockRequest.proposer_address 是造了这份提案的验证者地址 ≠ 已经正在造这份提案（不变量 429）；ProcessProposalResponse.status 是应用认为这份提案合法还是非法 ≠ 已经当成块非法（不变量 430）；FinalizeBlockResponse.events 是给索引用的类型键值事件 ≠ 已经印进本头（不变量 431）；FinalizeBlockResponse.consensus_param_updates 是对 gas、大小和其它共识相关参数的改动 ≠ 已经在块 H 生效（不变量 432）；VerifyVoteExtensionResponse.status 是应用认为这份扩展合法还是非法 ≠ 已经当成块非法（不变量 433）；VerifyStatus 的 UNKNOWN 一律是错、引擎当应用坏了会崩 ≠ 已经验过扩展（不变量 434）；Precommit 没有带有效签的扩展就会当非法丢掉、不调 Verify ≠ 已经跳过 Verify（不变量 435）；VerifyVoteExtensionRequest.validator_address 是签了这份扩展的验证者地址 ≠ 已经带了公钥（不变量 436）；ExtendVoteResponse.vote_extension 只会挂在非 nil Precommit 上、precommit nil 不会叫 ExtendVote ≠ 已经会调 ExtendVote（不变量 437）；应用回 extension 后 CometBFT 会填进 CanonicalVoteExtension、填其它字段并签名 ≠ 已经按原样签（不变量 438）；VerifyVoteExtension When late-arriving MAY add without Verify ≠ 迟到扩展 bundled / 已经 Verify 过 / 已经又叫了 Verify（不变量 518）；PrepareProposal When +2/3 late extensions not verified ≠ 迟到扩展 bundled / 已经 Verify 过 / 引擎会再 Verify（不变量 519）；PrepareProposal When suggested validate like Verify ≠ 迟到扩展 bundled / 引擎会再 Verify / 已经 Accept（不变量 520）；VerifyVoteExtension Usage empty extension still calls Verify ≠ Verify Usage bundled / 已经跳过 Verify / 已经 0 长就不叫 Verify（不变量 521）；VerifyVoteExtension Usage not called for local process ≠ Verify Usage bundled / 已经本地票也 Verify / 已经 Accept（不变量 522）；ExtendVote Usage precommit nil will not call ExtendVote ≠ bundled / 已经会调 ExtendVote / 已经签了 nil 票仍带扩展（不变量 524）；VerifyVoteExtension Usage hash does not guarantee Process ≠ Verify Usage bundled / 已经 Process 过 / 提议者也会叫 Process（不变量 523）；must provide values not already changed set / settled 正式三事（477 余量）≠ 已经改了集合 / 已经交差 / finasresult bundled（不变量 594）；as a result of executing not candidate / Process already ran 正式三事（477 余量）≠ 已经 Process 跑过就不用再执行 / 已经 apply candidate / finasresult bundled（不变量 595）；provided values not empty keep current / not CheckTx 正式三事（477 余量）≠ 已经空着就没有 must provide 义务 / 已经 CheckTx 过了 / finasresult bundled（不变量 596）；empty keep current not no must provide obligation 正式三事（458 余量）≠ 已经空着就没有 must provide 义务 / 已经 finempty bundled / nil 就什么也不做（不变量 597）；empty keep current not changed set / H+1 effective 正式三事（458 余量）≠ 已经 empty means no validator set / 已经改了集合 / H+1 换人 / finempty bundled（不变量 598）；empty consensus_param_updates keep current not H+1 effective 正式三事（458 余量）≠ 已经清掉参数 / 已经在 H 生效 / finempty bundled / 471 fincparam（不变量 599） |
| Ethereum | 3 | 重要 | 第一版；blob gas ≠ 执行气（不变量 145）；处理完一块 ≠ 已经改规范头（不变量 149）；提款操作 ≠ 用户交易（不变量 154）；父信标根 ≠ 当前头（不变量 156）；PREVRANDAO ≠ 工作量 / 无偏骰子（不变量 157）；基础费 ≠ 小费 / 烧掉 ≠ MEV 已解决（不变量 158）；瞬时存储 ≠ 账户持久存储（不变量 159）；后来的 SELFDESTRUCT ≠ 账户已经删掉（不变量 160）；JSON chainId ≠ 已经编进签名哈希（不变量 161）；发送者已有代码 ≠ 已经能当 EOA 发交易（不变量 162）；类型信封 ≠ 已经解开内层（不变量 167）；列入访问列表 ≠ 已经访问过（不变量 168）；本笔第一次碰 ≠ 已经热（不变量 169）；nonce 顶到规范上限 ≠ 已经还能加一（不变量 175）；initcode 超界 ≠ 已经是部署代码超界（不变量 176）；带回剩余气的回滚 ≠ 已经烧光剩余气（不变量 177）；静态帧 ≠ 已经是高级语言只读（不变量 178）；空 ≠ 已经不存在（不变量 180）；创建结束返回的运行时代码超界 ≠ 已经是 initcode 超界（不变量 185）；出块者地址开跑时已在热集合 ≠ 已经访问过（不变量 187）；新创建要存上链的代码以保留首字节开头 ≠ 已经是对象格式已经部署（不变量 188）；看见授权名单 ≠ 已经委托成功（不变量 190）；看见头里的请求承诺 ≠ 已经由共识层处理完（不变量 192）；看见执行层退出请求 ≠ 已经退出（不变量 193）；看见执行层存款日志 ≠ 已经由共识层办完（不变量 194）；看见状态里的历史执行哈希 ≠ 已经是 BLOCKHASH（不变量 195）；看见合并请求 ≠ 已经并成一把（不变量 196）；看见 calldata 地板 ≠ 已经改了执行气（不变量 197）；委员会下标被挪出签名 ≠ 已经没有委员会（不变量 198）；看见 BLS12-381 预编译 ≠ 已经在验 BLS 签（不变量 199）；看见抬高 blob 目标/上限 ≠ 已经改了两套气 / 已经是 PeerDAS（不变量 200）；看见 blob 底价 ≠ 已经并成一套气 / 已经改了日程（不变量 201）；看见 RLP 编码硬帽 ≠ 已经改了气限（不变量 202）；看见单笔气帽 ≠ 已经改了块气限（不变量 203）；看见 P256 验签预编译 ≠ 已经在验 k1（不变量 204）；看见 RANDAO 种子已知 ≠ 已经锁死出块日程（不变量 205）；看见 MODEXP 输入长度帽 ≠ 已经改了计价（不变量 206）；看见对等节点宣布历史窗 ≠ 已经改了共识历史（不变量 207）；看见数前导零操作码 ≠ 已经更便宜的 ZK 证明（不变量 208）；看见只改 blob 参数的专用分叉 ≠ 已经改了执行规则（不变量 209）；看见分叉配置 RPC 对上了 ≠ 已经过多客户端同根（不变量 210）；看见客户端默认气限 ≠ 已经是协议帽（不变量 211）；看见块级访问名单 ≠ 已经并行跑完（不变量 212）；看见签过的自愿退出 ≠ 已经永远有效（不变量 213）；看见加长证明纳入窗 ≠ 已经有确认规则（不变量 214）；看见激活流失帽 ≠ 已经改了奖励曲线（不变量 215）；看见内存拷贝指令 ≠ 已经是身份预编译 / 已经必须真分配中间缓冲（不变量 216）；看见压零指令 ≠ 已经是带立即数的压 0 / 已经改了跳转目的分析（不变量 217）；看见基础费指令 ≠ 已经改了费用市场 / 已经给了出块者（不变量 218）；看见 blob 基础费指令 ≠ 已经是执行层基础费指令 / 已经并成一套气（不变量 219）；看见链号指令 ≠ 已经是签进哈希的链号 / 已经是这笔带了 155 标识（不变量 220）；看见代码哈希指令 ≠ 已经看见代码本身 / 返回 0 ≠ 已经是没代码（不变量 221）；看见盐创建指令 ≠ 已经是按序号占址 / 算出来的盐地址 ≠ 已经创建（不变量 222）；看见退款削减 ≠ 已经没有退款（不变量 223）；看见弃用警告 ≠ 已经改了共识行为（不变量 224）；看见净计量 ≠ 已经是瞬时存储（不变量 225）；看见非零 calldata 降价 ≠ 已经没有块大小上限（不变量 226）；看见模幂重计价 ≠ 已经是 198 那道复杂度公式（不变量 227）；看见 bn128 预编译降价 ≠ 已经在验签（不变量 228）；看见本账户余额指令 ≠ 已经是按地址查余额（不变量 229）；看见 BLAKE2 压缩函数 F ≠ 已经是哈希（不变量 230）；看见原生移位指令 ≠ 已经用算术拼过移位（不变量 231）；看见返回数据缓冲 ≠ 已经是内存（不变量 232）；看见委托调用 ≠ 已经是 CALLCODE（不变量 233）；看见 Homestead ≠ 已经改了 CREATE / 已经拒高 s / 已经限制代码 / 已经没有炸弹（不变量 234）；看见忽略版本 / 多余字段 ≠ 已经在说新协议（不变量 235）；看见收据状态码 ≠ 已经能从剩余气推断成功（不变量 236）；看见读树涨价 ≠ 已经换成去掉六十四分之一 / 问超了 ≠ 已经耗尽气（不变量 237）；看见难度把叔块算进去 ≠ 已经按个数调 / 已经没有炸弹（不变量 238）；看见分叉标识对上 ≠ 已经同一条链 / 通告了下一分叉 ≠ 已经兼容（不变量 239）；看见签过的节点记录 ≠ 已经是最新一份 / 能多写键 ≠ 已经换了身份方案（不变量 240）；看见 ping 里的记录序号 ≠ 已经有当前记录 / 能发请求 ≠ 已经解析 / FindNode 找到人 ≠ 已经有记录（不变量 241） |
| Avalanche | 4 | 重要（对照） | 第一版 |
| Solana / Sui / Aptos | 5 | 重要 | 第一版；`store` ≠ 顶层资源（不变量 151） |
| Celestia / Polkadot | 6 | 重要 | 第一版 |
| Zcash / Monero / Mina | 7 | 进阶 | 第一版 |
| 乐观 Rollup | 8 | 重要 | 品类第一版；`unsafe` ≠ 已从 L1 推导（不变量 141）；DACert ≠ 已贴文（不变量 142） |
| Algorand | 8 | 进阶 | 抽签第一版 |
| Kaspa | 8 | 进阶 | 块 DAG 第一版 |
| Fuel | 8 | 进阶 | 思想级；谓词 ≠ 脚本（不变量 143） |
| Cardano | 8 | 进阶 | 仅 eUTXO 思想；引用输入 ≠ 已经花费（不变量 150）；无 19 节 |
| Nervos CKB | 8 | 进阶 | 思想级（占用） |
| NEAR Nightshade | 8 | 进阶 | 思想级（一条链+chunk） |
| Babylon | 8 | 进阶 | 仅过滤器页；k-deep ≠ commit（不变量 139）；无 19 节 |
| QRL | 8 | 研究级 | 仅过滤器页（XMSS / OTS） |
| Monad | 8 | 进阶 | 仅过滤器页（先定序再 Apply） |
| EigenLayer | 8 | 进阶 | 仅过滤器页；AVS 罚没 ≠ Casper（不变量 140）；无 19 节 |
| Starknet | 8 | 研究级 | 仅过滤器页（SNOS 程序哈希 + 四档最终性）；精读不变量 138 |

额外项目只在通过独特思想过滤器后建档。

---

## 不算资产的东西

- 币价、市值、空投
- 官网首页口号
- 无出处的 TPS 对比表
- 没有假设、没有代价、没有攻击面的「优点列表」
- 只列目录没有课文的「知识树完成」假象