# Level 1 · 工程密码学

优先级：必学  
先修：Level 0  
位置：课程轨。这里不讲币，不讲交易策略。

本层只回答一个问题：

> 这些密码零件各自挡住谁？挡住以后又会引入什么新成本？

先工程，后数学。公式和安全证明后置。

| 课 | 文件 | 覆盖 | 核心问题 |
|---|---|---|---|
| 1.1 | [L01-M01-hash.md](L01-M01-hash.md) | M1.1 | 哈希解决什么，撞了会怎样 |
| 1.2 | [L01-M02-signatures.md](L01-M02-signatures.md) | M1.2 | 签名如何把「授权」钉死；JSON chainId ≠ 已经编进签名哈希（不变量 161）；看见 BLS12-381 预编译 ≠ 已经在验 BLS 签（不变量 199）；看见 P256 验签预编译 ≠ 已经在验 k1（不变量 204）；看见签过的自愿退出 ≠ 已经永远有效（不变量 213）；看见链号指令 ≠ 已经是签进哈希的链号（不变量 220）；看见签过的消息 ≠ 已经控制资金（不变量 258） |
| 1.3 | [L01-M03-merkle.md](L01-M03-merkle.md) | M1.3 | 为什么轻节点能少下数据 |
| 1.4 | [L01-M04-canonical-encoding.md](L01-M04-canonical-encoding.md) | M1.4 | 同一对象两种写法如何分裂共识；类型字节 ≠ 已经解开内层（不变量 167）；ECDSA 验得过 ≠ 已经是严格 DER（不变量 172）；看见 Bech32 地址串 ≠ 链上已经有这笔输出（不变量 174）；看见部分签名包 ≠ 已经能广播（不变量 179）；后继校验过了 ≠ 已经是旧校验那套地址（不变量 181）；看见扩展公钥 ≠ 已经能花（不变量 182）；看见助记词 ≠ 已经是二进制种子（不变量 183）；看见私钥或助记词备份 ≠ 已经知道该看哪种输出脚本（不变量 184）；看见后继版本工作包 ≠ 已经是旧版那份固定未签交易（不变量 186）；看见多余栈元素 ≠ 已经随便填（不变量 264）；看见 BIP32 compatible ≠ 已经能互操作（不变量 266）；看见同一份种子 ≠ 已经是同一条币 / 余额为零 ≠ 已经发现完（不变量 267）；看见同一套 BIP44 账户 ≠ 已经能找回嵌套隔离见证（不变量 268）；看见脚本类型层 ≠ 已经是账户层（不变量 269）；看见同一套钥 ≠ 已经是同一条 P2SH 地址（不变量 270）；看见共享主公钥 ≠ 已经是本页 / 前面分支没有交易 ≠ 已经发现完（不变量 271）；看见派生钥 ≠ 已经是输出钥 / 不需要脚本路径 ≠ 已经不承诺（不变量 272）；看见付款码 ≠ 已经是存款地址 / 通知输出 ≠ 已经能花（不变量 273）；看见 multi ≠ 已经按字典序排 / 门限 ≠ 已经同一套上限（不变量 274）；看见 tr 没有树 ≠ 已经有脚本路径 / 压缩钥 ≠ 已经是 x-only（不变量 275）；看见 pk ≠ 已经和 pkh / sh 同一套放置 / 熟悉脚本 ≠ 已经能互操作（不变量 276）；看见 wpkh / wsh ≠ 已经只能顶层 / 未压缩钥 ≠ 已经允许（不变量 277）；看见 multi_a ≠ 已经是 383 那种 multi / 门限 ≠ 已经同一套编码（不变量 278）；看见旧 PSBT 栏 ≠ 已经能装 Taproot / 输出脚本里的钥 ≠ 已经是内部钥（不变量 279）；看见钱包策略 ≠ 已经是一条描述符 / 登记过 ≠ 已经批准这笔花（不变量 280）；看见 combo ≠ 已经只能产出一种脚本 / 未压缩钥 ≠ 已经带齐见证对（不变量 281）；看见 raw ≠ 已经能套进具名表达式 / addr ≠ 已经是那份输出脚本（不变量 282）；看见聚合钥 ≠ 已经是扩展公钥 / 合成扩展公钥 ≠ 已经能硬化派生（不变量 283）；看见旧 PSBT 栏 ≠ 已经能装 MuSig2 / 聚合钥栏 ≠ 已经是输出钥（不变量 284）；看见脚本各走各的路径 ≠ 已经是多签该有的树 / 路径里的脚本类型 ≠ 已经必要（不变量 285）；看见一份助记词 ≠ 已经能备齐所有钱包 / 派生出的熵 ≠ 已经是目标钱包的种子（不变量 286）；看见部分签名包 ≠ 已经是跨厂安全多签开户 / TOKEN ≠ 已经是钱包种子（不变量 287）；看见一条派生路径 ≠ 已经是一份路径模板 / 写死熟路径 ≠ 已经能互操作（不变量 288）；看见共享了扩展公钥 ≠ 已经是链码委托 / 本笔微调 ≠ 已经是盲签（不变量 289）；带 pj= 的付款 URI ≠ 已经是 payjoin 付款 / 原始包 ≠ 已经是提案 / 收款方加了输入 ≠ 已经另开一笔（不变量 290） |
| 1.5 | [L01-M05-pq-preview.md](L01-M05-pq-preview.md) | M1.6 | 后量子先改哪些工程账 |
| 1.6 | [L01-M06-randomness-and-determinism.md](L01-M06-randomness-and-determinism.md) | M1.5 | 三种 nonce 与 `Apply` 禁骰；PREVRANDAO ≠ 工作量 / 无偏骰子（不变量 157）；看见 RANDAO 种子已知 ≠ 已经锁死出块日程（不变量 205）；用户自造句子 ≠ 已经是本页那种助记词（不变量 183） |

试题后置，见 `exams/`。
