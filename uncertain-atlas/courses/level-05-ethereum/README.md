# Level 5 · Ethereum：可编程与多客户端

优先级：重要  
先修：L2.2 账户、L1.4 规范编码、L4.1 最终性对照（知道 CometBFT commit 长什么样）  
档案：[`../../protocols/ethereum/`](../../protocols/ethereum/README.md)

毕业：能解释「多个独立客户端如何仍对同一交易得到同一状态根」，并能把 head / justified / finalized 分开。

本课不教写 Solidity，不背官网 TPS。

| 课 | 文件 | 核心问题 |
|---|---|---|
| 5.1 | [L05-M01-evm-and-gas.md](L05-M01-evm-and-gas.md) | 任意程序如何仍是确定性状态机；blob gas ≠ 普通执行 gas（不变量 145）；基础费 ≠ 小费 / 烧掉 ≠ 已经给了出块者（不变量 158）；瞬时存储 ≠ 账户持久存储（不变量 159）；后来的 SELFDESTRUCT ≠ 账户已经删掉（不变量 160）；发送者已有代码 ≠ 已经能当 EOA 发交易（不变量 162）；类型信封 ≠ 已经解开内层（不变量 167）；列入访问列表 ≠ 已经访问过（不变量 168）；本笔第一次碰 ≠ 已经热（不变量 169）；nonce 顶到规范上限 ≠ 已经还能加一（不变量 175）；initcode 超界 ≠ 已经是部署代码超界（不变量 176）；带回剩余气的回滚 ≠ 已经烧光剩余气（不变量 177）；静态帧 ≠ 已经是高级语言只读（不变量 178）；空 ≠ 已经不存在（不变量 180）；创建结束返回的运行时代码超界 ≠ 已经是 initcode 超界（不变量 185）；出块者地址开跑时已在热集合 ≠ 已经访问过（不变量 187）；新创建要存上链的代码以保留首字节开头 ≠ 已经是对象格式已经部署（不变量 188）；看见授权名单 ≠ 已经委托成功（不变量 190）；看见状态里的历史执行哈希 ≠ 已经是 BLOCKHASH（不变量 195）；看见 calldata 地板 ≠ 已经改了执行气（不变量 197）；看见 BLS12-381 预编译 ≠ 已经在验 BLS 签（不变量 199）；看见 RLP 编码硬帽 ≠ 已经改了气限（不变量 202）；看见单笔气帽 ≠ 已经改了块气限（不变量 203）；看见 P256 验签预编译 ≠ 已经在验 k1（不变量 204）；看见 MODEXP 输入长度帽 ≠ 已经改了计价（不变量 206）；看见数前导零操作码 ≠ 已经更便宜的 ZK 证明（不变量 208）；看见客户端默认气限 ≠ 已经是协议帽（不变量 211 ≠ 203 ≠ 202 ≠ 96） |
| 5.2 | [L05-M02-el-cl-finality.md](L05-M02-el-cl-finality.md) | 执行层和共识层各保证什么；head ≠ justified ≠ finalized；处理完一块 ≠ 已经改规范头；看见头里的请求承诺 ≠ 已经由共识层处理完（不变量 192）；看见执行层退出请求 ≠ 已经退出（不变量 193）；看见执行层存款日志 ≠ 已经由共识层办完（不变量 194）；看见合并请求 ≠ 已经并成一把（不变量 196）；看见状态里的历史执行哈希 ≠ 已经是 BLOCKHASH（不变量 195）；委员会下标被挪出签名 ≠ 已经没有委员会（不变量 198）；看见 RANDAO 种子已知 ≠ 已经锁死出块日程（不变量 205）；提款操作 ≠ 用户交易；父信标根 ≠ 当前头；PREVRANDAO ≠ 工作量 / 无偏骰子；终局推迟 ≠ 停链 / leak ≠ slash |
| 5.3 | [L05-M03-multi-client.md](L05-M03-multi-client.md) | 多实现为什么是药也是刀；看见 RLP 编码硬帽 ≠ 已经是通道尺寸（不变量 202 ≠ 96）；看见单笔气帽 ≠ 已经改了块气限（不变量 203 ≠ 96）；看见对等节点宣布历史窗 ≠ 已经改了共识历史（不变量 207 ≠ 23 ≠ 25 ≠ 195）；看见分叉配置 RPC 对上了 ≠ 已经过多客户端同根（不变量 210 ≠ 149 ≠ 209 ≠ 207）；看见客户端默认气限 ≠ 已经是协议帽（不变量 211 ≠ 203 ≠ 96） |
| 5.4 | [L05-M04-state-blobs-mev.md](L05-M04-state-blobs-mev.md) | 状态胀了、blob、mempool 不再是队列；烧掉基础费 ≠ MEV 已经解决（不变量 158）；看见抬高 blob 目标/上限 ≠ 已经改了两套气 / 已经是 PeerDAS（不变量 200）；看见 blob 底价 ≠ 已经并成一套气（不变量 201）；看见只改 blob 参数的专用分叉 ≠ 已经改了执行规则（不变量 209 ≠ 200 ≠ 23） |

覆盖声明：L5.1→M5.1；L5.2→M5.3；L5.3→M5.4；L5.4→M5.2/M5.5/M5.6；M5.7→L9.9 方法 + 博物馆 CVE-2021-39137（官方 GHSA / postmortem）+ Sepolia 2024-03 Engine API 尺寸（不变量 96）+ RLP 编码硬帽≠气限 / ≠通道尺寸（不变量 202）+ 单笔气帽≠块气限（不变量 203）+ 对等历史窗≠已改共识（不变量 207）+ 分叉配置 RPC≠已同根（不变量 210）+ 默认气限≠协议帽（不变量 211）+ 2021-05 状态问题 / gas≠墙钟（不变量 101）+ 2016-11 OOG≠空账户删除已回滚（不变量 103）+ CVE-2025-30147 子群≠在曲线上（不变量 116）+ Avalanche 2025 正确语义≠预编译信任（不变量 119；C-Chain 是 EVM 事故，不是 Snow 抽样）。
