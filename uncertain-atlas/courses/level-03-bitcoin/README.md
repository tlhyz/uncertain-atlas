# Level 3 · Bitcoin 作为完整系统

优先级：必学  
先修：L1 + L2.1  
档案：[`../../protocols/bitcoin/report.md`](../../protocols/bitcoin/report.md)

| 课 | 文件 | 覆盖知识树 | 核心问题 |
|---|---|---|---|
| 3.1 | [L03-M01-nakamoto-finality.md](L03-M01-nakamoto-finality.md) | M3.1 | 概率最终为何仍能结算；进了块的 coinbase ≠ 已经能花（不变量 163）；coinbase 高度 ≠ 头上已经有高度字段（不变量 173）；脚本里的 CLTV ≠ 交易 nLockTime 已经把输出锁住（不变量 164）；脚本里的 CSV ≠ 绝对锁 / ≠ 部署名（不变量 165）；同一交易标识 ≠ 已经唯一（不变量 257） |
| 3.2 | [L03-M02-fees-and-standardness.md](L03-M02-fees-and-standardness.md) | M3.3 | 费用与标准性不是共识；策略拒绝 ≠ 共识非法（不变量 144）；进了块的手续费 ≠ 矿工已经能花（不变量 163）；选择加入替换信号 ≠ 已经换掉（不变量 166）；跳过库存通告 ≠ 已经拒进池（不变量 245）；内存池查询回了一串库存 ≠ 已经有那些交易（不变量 253）；拒收消息 ≠ 已经共识非法（不变量 254） |
| 3.3 | [L03-M03-conservative-evolution.md](L03-M03-conservative-evolution.md) | M3.4 方向 | 慢升级本身是安全特性；版本位被置上 ≠ 已经锁定；锁定 ≠ 已经激活（不变量 171）；ECDSA 验得过 ≠ 已经是严格 DER（不变量 172）；看见多余栈元素 ≠ 已经随便填（不变量 264） |
| 3.4 | [L03-M04-network-and-eclipse.md](L03-M04-network-and-eclipse.md) | M3.2 | 传播与日蚀；privatebroadcast ≠ IP 已藏；后继地址流言 ≠ 已经连得上（不变量 246）；发了 sendheaders ≠ 已经有块（不变量 247）；按 wtxid 通告 ≠ 已经有交易（不变量 248）；对账素描 ≠ 已经有交易（不变量 249）；带见证的线上序列化 ≠ 已经有见证（不变量 251）；版本里关掉交易转发 ≠ 已经终身只传块（不变量 256）；协议版本够了 ≠ 已经支持某项功能（不变量 259）；看见 pong ≠ 已经还活着（不变量 262）；看见协议版本 ≠ 已经是客户端版本（不变量 263） |
| 3.5 | [L03-M05-full-node-and-spv.md](L03-M05-full-node-and-spv.md) | M3.5 | 全节点 / 剪枝 / SPV；修剪合取 ≠ 修剪非法；有限服务位 ≠ 已经能服任意旧块（不变量 250）；没开布隆服务位 ≠ 已经退役（不变量 252） |
| 3.6 | [L03-M06-testing-and-culture.md](L03-M06-testing-and-culture.md) | M3.6 / M3.7 方法 | 事故如何回流成规则；迁移失败 ≠ 邻居已安全；看见部分签名包 ≠ 已经能广播（不变量 179）；看见扩展公钥 ≠ 已经能花（不变量 182）；看见助记词 ≠ 已经是二进制种子（不变量 183）；看见私钥或助记词备份 ≠ 已经知道该看哪种输出脚本（不变量 184）；看见后继版本工作包 ≠ 已经是旧版那份固定未签交易（不变量 186）；看见 Miniscript ≠ 已经是链上脚本（不变量 191）；看见付款 URI ≠ 已经授权（不变量 255）；看见签过的消息 ≠ 已经控制资金（不变量 258）；看见静默付款地址 ≠ 已经有输出（不变量 260）；看见可读名字 ≠ 已经该走 DNS（不变量 261）；看见 signet ≠ 已经是 testnet / 已经是 regtest / 已经签过（不变量 265）；看见 BIP32 compatible ≠ 已经能互操作（不变量 266）；看见同一份种子 ≠ 已经是同一条币 / 余额为零 ≠ 已经发现完（不变量 267）；看见同一套 BIP44 账户 ≠ 已经能找回嵌套隔离见证（不变量 268）；看见脚本类型层 ≠ 已经是账户层（不变量 269）；看见同一套钥 ≠ 已经是同一条 P2SH 地址（不变量 270）；看见共享主公钥 ≠ 已经是本页 / 前面分支没有交易 ≠ 已经发现完（不变量 271）；看见派生钥 ≠ 已经是输出钥 / 不需要脚本路径 ≠ 已经不承诺（不变量 272）；看见付款码 ≠ 已经是存款地址 / 通知输出 ≠ 已经能花（不变量 273）；看见 multi ≠ 已经按字典序排 / 门限 ≠ 已经同一套上限（不变量 274）；看见 tr 没有树 ≠ 已经有脚本路径 / 压缩钥 ≠ 已经是 x-only（不变量 275）；看见 pk ≠ 已经和 pkh / sh 同一套放置 / 熟悉脚本 ≠ 已经能互操作（不变量 276）；看见 wpkh / wsh ≠ 已经只能顶层 / 未压缩钥 ≠ 已经允许（不变量 277）；看见 multi_a ≠ 已经是 383 那种 multi / 门限 ≠ 已经同一套编码（不变量 278）；看见旧 PSBT 栏 ≠ 已经能装 Taproot / 输出脚本里的钥 ≠ 已经是内部钥（不变量 279）；看见钱包策略 ≠ 已经是一条描述符 / 登记过 ≠ 已经批准这笔花（不变量 280）；看见 combo ≠ 已经只能产出一种脚本 / 未压缩钥 ≠ 已经带齐见证对（不变量 281）；看见 raw ≠ 已经能套进具名表达式 / addr ≠ 已经是那份输出脚本（不变量 282）；看见聚合钥 ≠ 已经是扩展公钥 / 合成扩展公钥 ≠ 已经能硬化派生（不变量 283）；看见旧 PSBT 栏 ≠ 已经能装 MuSig2 / 聚合钥栏 ≠ 已经是输出钥（不变量 284）；看见脚本各走各的路径 ≠ 已经是多签该有的树 / 路径里的脚本类型 ≠ 已经必要（不变量 285）；看见一份助记词 ≠ 已经能备齐所有钱包 / 派生出的熵 ≠ 已经是目标钱包的种子（不变量 286）；看见部分签名包 ≠ 已经是跨厂安全多签开户 / TOKEN ≠ 已经是钱包种子（不变量 287）；看见一条派生路径 ≠ 已经是一份路径模板 / 写死熟路径 ≠ 已经能互操作（不变量 288）；看见共享了扩展公钥 ≠ 已经是链码委托 / 本笔微调 ≠ 已经是盲签（不变量 289）；带 pj= 的付款 URI ≠ 已经是 payjoin 付款 / 原始包 ≠ 已经是提案 / 收款方加了输入 ≠ 已经另开一笔（不变量 290）；自家习惯的输入输出顺序 ≠ 已经是字典序标准 / 按字典序排了 ≠ 已经是共识 / ≠ 已经私人（不变量 291）；Testnet 4 ≠ 已经是 Testnet 3 / 20 分钟例外 ≠ 已经没有块风暴 / 会 Testnet 3 ≠ 已经能安全跟（不变量 292）；储备证明交易 ≠ 已经能花 / 其余输入签过 ≠ 已经控制资金 / POR 栏 ≠ 已经是普通花费（不变量 293）；本页这种签消息 ≠ 已经是 322 / 头字节标了种类 ≠ 已经有地址 / 旧 P2PKH 习惯 ≠ 已经互操作（不变量 294）；付款请求 ≠ 已经授权 / 付款报文 ≠ 已经是回执 / 回执 ≠ 已经最终（不变量 295） |
| 3.7 | [L03-M07-segwit-soft-fork.md](L03-M07-segwit-soft-fork.md) | M3.4 结构 | 见证如何软分叉进旧验证；txid ≠ wtxid（不变量 152）；按 wtxid 通告 ≠ 已经有交易（不变量 248）；带见证的线上序列化 ≠ 已经有见证（不变量 251）；钥匙路径 ≠ 揭树（不变量 153）；走脚本路径 ≠ 已经是 tapscript 语义（不变量 189）；看见 Miniscript ≠ 已经是链上脚本（不变量 191）；付给脚本哈希 ≠ 已经揭开赎回脚本（不变量 170）；看见 Bech32 地址串 ≠ 链上已经有这笔输出（不变量 174）；后继校验过了 ≠ 已经是旧校验那套地址（不变量 181）；看见多余栈元素 ≠ 已经随便填（不变量 264） |
| 3.8 | [L03-M08-block-dag.md](L03-M08-block-dag.md) | Kaspa 对照 | 孤块为何出现、DAG 如何仍要全序 |

Tapscript 叶子语义见不变量 189。Miniscript 见不变量 191。钥匙路径 ≠ 揭树见不变量 153。付给脚本哈希 ≠ 已经揭开赎回脚本见不变量 170。2013 分叉七问见博物馆 BIP 50。
