# 例：看见派生钥不是已经是输出钥；看见不需要脚本路径不是已经不承诺；看见种子备份不是已经能找回单钥 P2TR

**层次**：应用 / 规范对象。  
**分类**：事实（对象边界）+ 推断（产品）+ 建议（产品）。  
**来源**：Bitcoin [BIP-86](https://github.com/bitcoin/bips/blob/master/bip-0086.mediawiki)（Deployed, Applications）。  
**对应课文**：[L1.4](../../courses/level-01-crypto/L01-M04-canonical-encoding.md)、[L3.6](../../courses/level-03-bitcoin/L03-M06-testing-and-culture.md)。  
**不要写进**：Ethereum 行、L5.1、03 共识、M5.4、L5.4。本页是「派生钥不是已经是输出钥 / 不需要脚本路径不是已经不承诺」，不是钥匙路径已经揭开有没有树，也不是同一套 BIP44 账户已经能找回嵌套隔离见证。不要另写 BIP-84 当第二份 49 理由页。

## 官方三件事

规范把单钥 P2TR 派生写成三件独立的结构事，不是「看见同一份种子就已经能找回 Taproot」一件事：

1. **看见派生钥 / 看见 BIP32 路径 不是已经是输出钥，也不是已经是链上那 32 字节。**  
   官方写：本页先按与 44 / 49 / 84 同一套账户结构长出一把派生钥，再把这把派生钥当成内部钥。输出钥是内部钥再加上对内部钥做标签微调后的点。看见路径对上了，不是已经对上了输出钥。看见内部钥，不是已经是见证里那 32 字节。
2. **看见不需要脚本路径 / 看见单钥 不是已经不承诺脚本路径，也不是已经没有脚本路径。**  
   官方引用 341：若花费条件并不需要脚本路径，输出钥仍应当承诺一条不可花的脚本路径，而不是没有脚本路径。看见单钥，不是已经可以不微调。看见不需要脚本路径，不是已经是「没有脚本路径」那种输出。
3. **看见种子备份 / 看见已有描述符方案 不是已经能找回单钥 P2TR，也不是已经不必再写本页。**  
   官方写：许多软件钱包和硬件签名器仍只用种子备份，备份里没有派生路径和脚本信息。现在已有方案可以不再为每种脚本固定一条路径，但那些只备份种子的实现仍需要本页。本页按设计不向后兼容；不会本页的钱包根本发现不了这些账户，用户会察觉出事。看见种子备份，不是已经能找回单钥 Taproot。看见已有描述符，不是已经不必再写本页。

用途号、测试向量、地址例、怎样算标签微调是规范里的取值或做法，本页不抄。

## 官方为什么这样拆

- **派生钥 ≠ 已经是输出钥：** 官方把内部钥和输出钥写成两步。
- **不需要脚本路径 ≠ 已经不承诺：** 官方把「仍应当承诺一条不可花脚本路径」写成 341 的要求。
- **种子备份 ≠ 已经能找回：** 官方把「备份里没有路径和脚本信息」写成仍要本页的理由。

## 和相邻页的边界

| 对象 | 本页 | 那一页 |
|---|---|---|
| 派生钥 | 不是已经是输出钥 | 不是已经揭开有没有树（153） |
| 不需要脚本路径 | 不是已经不承诺 | 不是已经是 tapscript 语义（189） |
| 种子备份 | 不是已经能找回单钥 P2TR | 不是已经知道该看哪种脚本（184） |
| 同一套 44/49/84 结构 | 本页只说方法相同 | 不是已经能找回嵌套隔离见证（268） |

## 产品

**建议（产品，不是事实）**：不确定第一条结算机如果导出单钥后量子地址，必须分开派生钥、输出钥、承诺了没有。可以跳过「看见种子就已经能找回 Taproot」。若对照，必须分开内部钥和输出钥。不要另写怎样算标签微调，也不要另写 BIP-84。

## 本页不抄

- 用途号、测试向量、地址例、助记词例、扩展钥例。
- 怎样算标签微调、怎样 lift、怎样拼见证。
