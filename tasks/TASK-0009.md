# TASK-0009: ACCOUNTING_INVARIANTS + Property Tests

## Why
User spec §12: equity conservation must be verified every bar; silent accounting bugs invalidate all research.

## Hypothesis
Formal invariants + property tests catch drift before experiments scale.

## Dependencies
TASK-0005

## Implementation
1. Create `docs/ACCOUNTING_INVARIANTS.md` documenting formulas
2. Add `qtb/ab/accounting_check.py` — assert equity = cash + position + uPnL - fees + rebate + funding per bar
3. Property tests: 0 fee + flat price → no profit; no trades → equity unchanged; deterministic replay
4. Regression test for ts_ms 1970 bug

## Validation
- All property tests pass
- Existing 79 tests still green
- Invariant violation raises in test mode

## Failure Conditions
- Invariants documented but not tested
- Tests pass on broken engine

## Expected Output
`docs/ACCOUNTING_INVARIANTS.md`, `tests/test_accounting_invariants.py`, `tests/test_property_execution.py`

## Reviewer Checklist
- [ ] Perp isolated + cross formulas covered
- [ ] ETF spot path included
- [ ] Red team: can engine still drift?
