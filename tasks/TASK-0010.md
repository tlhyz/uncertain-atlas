# TASK-0010: test_no_future_leak.py

## Why
User spec §13: EMA, ATR, swing, regime, beta must use past data only.

## Hypothesis
Automated tests can detect centered windows and future bar indexing in signal code.

## Dependencies
TASK-0005

## Implementation
1. Create `tests/test_no_future_leak.py`
2. Test `qtb/dual/signals.py` — drawdown tiers, reversal R1–R5, EMA cross
3. Test `qtb/ab/regimes.py`, `src/features/regime.py`, `src/features/beta.py`
4. Method: shift future bar NaN → signal unchanged; or compare t vs t+1 isolation

## Validation
- All signal functions pass
- Adding `df.iloc[i+1]` in test fixture causes failure

## Failure Conditions
- Tests exist but don't catch obvious leak
- Signals untested

## Expected Output
`tests/test_no_future_leak.py` green

## Reviewer Checklist
- [ ] Tech FSM transitions tested
- [ ] Crypto FSM independence tested
- [ ] Parameter selection not using full-sample stats
