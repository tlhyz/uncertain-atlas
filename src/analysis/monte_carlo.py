"""Block bootstrap Monte Carlo — STEP 9 implementation placeholder."""

from __future__ import annotations

import numpy as np


def block_bootstrap(equity_curve: np.ndarray, block_days: int = 3, n_paths: int = 1000) -> dict:
    """Placeholder: returns empty percentiles until full experiment pipeline."""
    return {"n_paths": n_paths, "block_days": block_days, "status": "NOT_IMPLEMENTED"}
